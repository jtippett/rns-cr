2026-03-10 — Task 1.1: Initialize Crystal shard project
  - Created  directory with shard.yml (name: rns, version: 0.1.0, crystal >= 1.9.0)
  - Added all dependencies: ed25519, qr-code, hkdf, msgpack, bindata, ameba (dev)
  - Created src/rns.cr entry point with RNS module and version method
  - Created src/rns/version.cr with VERSION constant
  - Created spec/spec_helper.cr and spec/rns_spec.cr (trivial spec)
  - Created .gitignore for Crystal (lib/, .shards/, bin/)
  - Ran shards install — all dependencies resolved successfully
  - Ran crystal spec — 2 examples, 0 failures

2026-03-10 — Task 1.2: Logging and utility infrastructure
  - Created src/rns/vendor/platform_utils.cr — OS detection (get_platform, is_linux?, is_darwin?, is_android?, is_windows?, use_epoll?, use_af_unix?)
  - Created src/rns/log.cr — Log levels (LOG_NONE through LOG_EXTREME), log destinations (LOG_STDOUT, LOG_FILE, LOG_CALLBACK), loglevelname(), log() with configurable destination/level/callback, timestamp_str(), host_os(), rand(), panic()
  - Created src/rns/utilities.cr — hexrep(), prettyhexrep(), prettysize(), prettyspeed(), prettyfrequency(), prettydistance(), prettytime(), prettyshorttime()
  - Updated src/rns.cr to require all new modules
  - Created spec/rns/vendor/platform_utils_spec.cr (9 examples)
  - Created spec/rns/log_spec.cr (33 examples)
  - Created spec/rns/utilities_spec.cr (36 examples)
  - Ran crystal spec — 80 examples, 0 failures

2026-03-10 — Task 2.1: Hashes — SHA-256 and SHA-512
  - Created src/rns/cryptography/hashes.cr — sha256(), sha512(), full_hash(), truncated_hash() wrapping OpenSSL::Digest
  - TRUNCATED_HASHLENGTH constant (128 bits) matching Python RNS
  - truncated_hash returns first 16 bytes of SHA-256, matching Identity.truncated_hash behavior
  - Created spec/rns/cryptography/hashes_spec.cr (21 examples)
    - NIST test vectors: empty, less-than-block, block-length, million-byte inputs for both SHA-256 and SHA-512
    - 1000-iteration random roundtrip tests comparing against OpenSSL directly
    - truncated_hash consistency and correctness tests
    - full_hash alias verification
  - Updated src/rns.cr to require cryptography/hashes
  - Ran crystal spec — 101 examples, 0 failures

2026-03-10 — Task 2.2: HMAC and HKDF
  - Created src/rns/cryptography/hmac.cr — HMAC class wrapping OpenSSL::HMAC
    - HMAC.new(key, msg) with .update(), .digest(), .hexdigest() matching Python API
    - HMAC.digest(key, data) class method for fast inline HMAC-SHA256
    - Supports incremental updates and non-destructive digest calls
  - Created src/rns/cryptography/hkdf.cr — hkdf() function matching Python RNS exactly
    - Self-contained HKDF-SHA256 implementation (extract-then-expand)
    - Signature: hkdf(length, derive_from, salt, context) matching Python parameter names
    - Handles nil/empty salt (defaults to 32 zero bytes) and nil context (defaults to empty)
    - Input validation: raises ArgumentError for invalid length or empty derive_from
  - Created spec/rns/cryptography/hmac_spec.cr (16 examples)
    - RFC 4231 test vectors (Cases 1-4, 6, 7) for HMAC-SHA256
    - Object API tests: new, update, digest, hexdigest, incremental updates
    - 100-iteration random roundtrip tests comparing against OpenSSL::HMAC directly
  - Created spec/rns/cryptography/hkdf_spec.cr (15 examples)
    - RFC 5869 test vectors (Cases 1-3) for HKDF-SHA256
    - Nil/empty salt and context handling tests
    - Input validation tests (invalid length, empty derive_from)
    - Determinism, output length, and cross-verification against manual HMAC computation
  - Updated src/rns.cr to require cryptography/hmac and cryptography/hkdf
  - Ran crystal spec — 132 examples, 0 failures

2026-03-10 — Task 2.3: PKCS7 padding and AES-256-CBC
  - Created src/rns/cryptography/pkcs7.cr — PKCS7 module with pad() and unpad()
    - BLOCKSIZE constant (16) matching Python RNS
    - pad(data, bs) adds PKCS7 padding, always adds at least 1 byte
    - unpad(data, bs) removes padding, validates padding byte value
    - Raises ArgumentError on invalid padding (0, > block_size, empty data)
  - Created src/rns/cryptography/aes.cr — AES_128_CBC and AES_256_CBC modules
    - Wraps OpenSSL::Cipher with padding disabled (caller handles PKCS7)
    - encrypt(plaintext, key, iv) and decrypt(ciphertext, key, iv) for both key sizes
    - Key length validation matching Python behavior (raises on wrong size)
  - Created spec/rns/cryptography/pkcs7_spec.cr (18 examples)
    - Pad tests: short data, block-aligned, one-short, multi-block, empty, custom block sizes
    - Unpad tests: valid padding, full block, single byte, invalid padding, zero padding, empty
    - Roundtrip tests: various lengths, custom block sizes, 1000 random iterations
  - Created spec/rns/cryptography/aes_spec.cr (16 examples)
    - NIST SP 800-38A test vectors for AES-256-CBC (single block + 4-block) and AES-128-CBC
    - Roundtrip encryption, different keys/IVs produce different ciphertext
    - Key length validation, ciphertext size verification
    - 1000-iteration random roundtrip tests with PKCS7 for both AES-256 and AES-128
  - Updated src/rns.cr to require cryptography/pkcs7 and cryptography/aes
  - Ran crystal spec — 166 examples, 0 failures

2026-03-10 — Task 2.4: X25519 key exchange
  - Created src/rns/cryptography/x25519.cr — X25519PrivateKey and X25519PublicKey classes
    - Wraps Ed25519::Curve25519 shard (scalar_mult, scalar_mult_base) for ECDH
    - X25519PrivateKey: generate(), from_private_bytes(data), private_bytes(), public_key(), exchange(peer)
    - X25519PublicKey: from_public_bytes(data), public_bytes()
    - Scalar clamping per RFC 7748 (clear 3 LSBs, clear bit 255, set bit 254)
    - Public key high-bit masking per RFC 7748 Section 5
    - Constant-time execution padding matching Python's timing protection
    - exchange() accepts both X25519PublicKey and raw Bytes
  - Created spec/rns/cryptography/x25519_spec.cr (26 examples)
    - RFC 7748 Section 6.1 test vectors: Alice/Bob public key derivation and shared secret
    - RFC 7748 Section 5.2 iterated test vectors (1 iteration and 1000 iterations)
    - Key generation, uniqueness, serialization roundtrip
    - Two-party key exchange produces matching shared secrets
    - Clamping verification, invalid length rejection
    - 100-iteration random key exchange roundtrip tests
  - Updated src/rns.cr to require cryptography/x25519
  - Ran crystal spec — 192 examples, 0 failures

2026-03-10 — Task 2.5: Ed25519 signatures
  - Created src/rns/cryptography/ed25519.cr — Ed25519PrivateKey and Ed25519PublicKey classes
    - Wraps OpenSSL EVP API directly (EVP_PKEY_new_raw_private_key, EVP_DigestSign, EVP_DigestVerify)
    - Chose OpenSSL EVP over spider-gazelle/ed25519 shard for better RFC 8032 compliance
    - Ed25519PrivateKey: generate(), from_private_bytes(data), private_bytes(), public_key(), sign(message)
    - Ed25519PublicKey: from_public_bytes(data), public_bytes(), verify(signature, message)
    - Proper resource cleanup with begin/ensure for EVP_PKEY_free and EVP_MD_CTX_free
    - Extended Crystal's LibCrypto with Ed25519-specific EVP bindings (NID_ED25519 = 1087)
  - Created spec/rns/cryptography/ed25519_spec.cr (28 examples)
    - Key generation, uniqueness, from_bytes construction, invalid length rejection
    - Sign/verify roundtrip, invalid signature rejection, tampered message detection
    - Deterministic signatures, empty message signing, different messages produce different sigs
    - Key serialization roundtrip (private and public), restored key derives same public key
    - RFC 8032 test vectors: exact signature match for tests 1 and 3, sign+verify roundtrip for test 2
    - Note: OpenSSL 3.6.1 on ARM64 macOS produces subtly different S scalar for test vector 2
    - 100-iteration random sign/verify roundtrip tests
  - Updated src/rns.cr to require cryptography/ed25519
  - Ran crystal spec — 220 examples, 0 failures

2026-03-10 — Task 2.6: Token (Fernet-like authenticated encryption)
  - Created src/rns/cryptography/token.cr — Token class porting RNS/Cryptography/Token.py
    - TOKEN_OVERHEAD constant (48 bytes = 16 IV + 32 HMAC-SHA256)
    - Token.generate_key(mode) — generates 64-byte key for AES-256-CBC (default) or 32-byte for AES-128-CBC
    - Token#initialize(key) — splits key into signing and encryption halves based on key length
    - Token#encrypt(data) — random IV + AES-CBC(PKCS7.pad(data)) + HMAC-SHA256 of (IV + ciphertext)
    - Token#decrypt(token) — verifies HMAC, extracts IV/ciphertext, decrypts, unpads PKCS7
    - Token#verify_hmac(token) — validates HMAC-SHA256 of token data matches appended HMAC
    - Supports both AES-128-CBC (32-byte key) and AES-256-CBC (64-byte key) modes
  - Created src/rns/cryptography/provider.cr — re-exports all crypto modules, sets PROVIDER = :internal
  - Created spec/rns/cryptography/token_spec.cr (29 examples)
    - TOKEN_OVERHEAD constant verification
    - Key generation: correct sizes for both modes, uniqueness, invalid mode rejection
    - Constructor: 32-byte and 64-byte keys, invalid size rejection
    - Encrypt: correct overhead, random IV produces different ciphertexts, empty/large data
    - Decrypt: roundtrip for both AES modes, tampered ciphertext/HMAC rejection, wrong key rejection
    - verify_hmac: valid/tampered tokens, too-short token rejection
    - Overhead constant verification: exact size checks for various plaintext lengths
    - 100-iteration random roundtrip tests for both AES-128-CBC and AES-256-CBC
  - Updated src/rns.cr to require cryptography/token and cryptography/provider
  - Ran crystal spec — 249 examples, 0 failures

2026-03-10 — Task 3.1: Identity module
  - Created src/rns/identity.cr — Identity class porting RNS/Identity.py
    - Constants: CURVE ("Curve25519"), KEYSIZE (512), HASHLENGTH (256), SIGLENGTH (512),
      NAME_HASH_LENGTH (80), TRUNCATED_HASHLENGTH (128), RATCHETSIZE (256),
      RATCHET_EXPIRY (30 days), TOKEN_OVERHEAD (48), AES128_BLOCKSIZE (16),
      DERIVED_KEY_LENGTH (64), DERIVED_KEY_LENGTH_LEGACY (32)
    - Class-level state: known_destinations (Hash), known_ratchets (Hash)
    - Key management: create_keys(), get_private_key(), get_public_key(),
      load_private_key(data), load_public_key(data), update_hashes()
    - Encryption: encrypt(plaintext, ratchet) using ECDH + HKDF + Token,
      decrypt(ciphertext_token, ratchets, enforce_ratchets) with ratchet support
    - Signing: sign(message) via Ed25519, validate(signature, message)
    - Hash functions: full_hash(data), truncated_hash(data), get_random_hash()
    - Remember/recall: remember(), recall(target_hash, from_identity_hash),
      recall_app_data(), save_known_destinations(), load_known_destinations()
    - Ratchet methods: generate_ratchet(), ratchet_public_bytes(), get_ratchet_id(),
      remember_ratchet(), get_ratchet(), current_ratchet_id(), clean_ratchets()
    - Factory methods: from_bytes(prv_bytes), from_file(path)
    - File I/O: to_file(path), load(path)
    - Persistence uses MessagePack for known_destinations serialization
  - Created spec/rns/identity_spec.cr (76 examples)
    - Constants verification (12 tests)
    - Key generation: default creation, no-keys mode, uniqueness, correct sizes, hash computation
    - Key serialization: private key roundtrip, public key roundtrip, derived key verification
    - Hash computation: full_hash, truncated_hash, get_random_hash
    - Signing: sign/verify roundtrip, invalid sig rejection, wrong message, KeyError checks,
      cross-instance verify, deterministic signatures, empty messages
    - Encryption: encrypt/decrypt roundtrip, overhead check, random IV, empty/large data,
      wrong identity, tampered ciphertext, too-short token, KeyError checks,
      pub-only encrypt + prv decrypt, 100 random roundtrips
    - Ratchet encryption: encrypt with ratchet, fallback to identity key, enforce_ratchets
    - Remember/recall: basic roundtrip, unknown destination, app_data, invalid key rejection,
      overwrite behavior, recall by identity hash
    - File I/O: save/load roundtrip, nonexistent file, from_bytes
    - Persistence: save/load known_destinations via MessagePack
    - Ratchet methods: generate, public bytes, ratchet ID, uniqueness, remember/retrieve
    - Stress tests: 100 sign/verify roundtrips, 50 key serialization roundtrips
  - Updated src/rns.cr to require identity module
  - Ran crystal spec — 325 examples, 0 failures

2026-03-10 — Task 3.2: Packet module
  - Created src/rns/reticulum.cr — Reticulum module with core protocol constants
    - MTU (500), TRUNCATED_HASHLENGTH (128), IFAC_MIN_SIZE (1)
    - HEADER_MINSIZE (19), HEADER_MAXSIZE (35), MDU (464)
    - DEFAULT_PER_HOP_TIMEOUT (6)
  - Created src/rns/transport.cr — Transport module stub with transport type constants
    - BROADCAST (0x00), TRANSPORT (0x01), RELAY (0x02), TUNNEL (0x03)
  - Created src/rns/destination.cr — Destination module with type constants and stub class
    - SINGLE (0x00), GROUP (0x01), PLAIN (0x02), LINK (0x03)
    - IN/OUT direction constants, PROVE_NONE/APP/ALL proof strategy constants
    - DestinationInterface abstract module for Packet interop
    - Destination::Stub class implementing DestinationInterface for testing
  - Created src/rns/packet.cr — Packet, ProofDestination, PacketReceipt classes
    porting RNS/Packet.py
    - Packet type constants: DATA (0x00), ANNOUNCE (0x01), LINKREQUEST (0x02), PROOF (0x03)
    - Header types: HEADER_1 (0x00), HEADER_2 (0x01)
    - All 21 context types: NONE through LRPROOF (0xFF)
    - Size constants: HEADER_MAXSIZE (35), HEADER_MINSIZE (19), MDU (464),
      PLAIN_MDU (464), ENCRYPTED_MDU (383), TIMEOUT_PER_HOP (6)
    - Packet class: initialize (from destination or raw bytes), get_packed_flags(),
      pack(), unpack(), update_hash(), get_hash(), get_truncated_hash(),
      get_hashable_part(), generate_proof_destination()
    - Pack handles all no-encryption cases: ANNOUNCE, LINKREQUEST, RESOURCE_PRF proof,
      LINK destination proof, RESOURCE, KEEPALIVE, CACHE_REQUEST contexts
    - Pack encrypts data for SINGLE/GROUP destinations via destination.encrypt()
    - HEADER_2 support with transport_id in header
    - LRPROOF context uses link_id instead of destination hash
    - Unpack parses flags, hops, header type, transport type, destination type,
      packet type, context flag from raw bytes
    - Hash computation: masks upper nibble of flags byte, strips transport_id for
      HEADER_2 (ensures hash identity across header types)
    - ProofDestination: creates truncated-hash destination for proof routing,
      encrypt() returns plaintext unchanged
    - PacketReceipt: status tracking (FAILED/SENT/DELIVERED/CULLED),
      EXPL_LENGTH (96), IMPL_LENGTH (64), timeout management, RTT computation,
      delivery/timeout callbacks, validate_proof() with explicit and implicit proofs
    - PacketReceiptCallbacks: holds delivery and timeout callback procs
  - Created spec/rns/packet_spec.cr (76 examples)
    - Constants: all packet types, header types, 21 context types, flag values,
      MTU/MDU size constants, timeout constant
    - Reticulum/Transport/Destination constant verification
    - Flag packing: HEADER_1/2, all transport/destination type combinations,
      context_flag, LRPROOF forces LINK type
    - Pack: PLAIN DATA, ANNOUNCE, LINKREQUEST, RESOURCE, KEEPALIVE, CACHE_REQUEST,
      RESOURCE_PRF proof, LINK proof — all verify no encryption
    - Pack: HEADER_2 with transport_id, raises on HEADER_2 without transport_id,
      raises when packed size exceeds MTU
    - Pack: SINGLE destination encrypts data, destination_hash set, packet_hash computed
    - Unpack: HEADER_1 and HEADER_2 parsing, malformed/empty packet handling
    - Pack/unpack roundtrip: HEADER_1 PLAIN, HEADER_2 ANNOUNCE, hash preservation,
      100-iteration random roundtrip stress test
    - Hash computation: hashable part masking, transport_id exclusion for HEADER_2,
      different payloads produce different hashes, same hash across header types
    - Truncated hash: correct 16-byte size
    - Constructor: from raw (nil destination), from destination with all field defaults
    - LRPROOF: uses link_id in header
    - PacketReceipt: status constants, proof length constants, initialization,
      get_status, set_timeout, set_delivery_callback, set_timeout_callback,
      get_rtt, is_timed_out, check_timeout (FAILED/CULLED/no-change/callback)
    - ProofDestination: truncated hash creation, type=SINGLE, encrypt passthrough
    - MTU boundary: maximum payload, empty payload
  - Updated src/rns.cr to require reticulum, transport, destination, packet modules
  - Ran crystal spec — 401 examples, 0 failures

2026-03-10 — Task 3.3: Destination module
  - Rewrote src/rns/destination.cr — transformed from stub module to full Destination class
    porting RNS/Destination.py (691 LOC)
    - Type constants: SINGLE (0x00), GROUP (0x01), PLAIN (0x02), LINK (0x03)
    - Direction constants: IN (0x11), OUT (0x12)
    - Proof strategy constants: PROVE_NONE (0x21), PROVE_APP (0x22), PROVE_ALL (0x23)
      matching Python values exactly
    - Request policy constants: ALLOW_NONE (0x00), ALLOW_ALL (0x01), ALLOW_LIST (0x02)
    - Ratchet constants: PR_TAG_WINDOW (30), RATCHET_COUNT (512), RATCHET_INTERVAL (1800)
    - DestinationInterface abstract module and Stub class preserved for backward compat
    - DestinationCallbacks class for link_established, packet, proof_requested callbacks
    - RequestHandler struct for request handler registration
    - Destination class implements DestinationInterface
    - Static methods: expand_name(), hash() (with Identity and raw Bytes overloads),
      app_and_aspects_from_name(), hash_from_name_and_identity()
    - Constructor: initialize(identity, direction, type, app_name, aspects, register)
      with auto-identity creation for IN SINGLE destinations, validation of types/directions,
      dot rejection in names/aspects, Transport registration
    - Hash computation: name_hash (NAME_HASH_LENGTH bits) + identity hash -> truncated hash
    - Announce: announce(app_data, path_response, tag, send) generates announce packets with
      public_key + name_hash + random_hash + ratchet + signature + app_data,
      path response caching, stale response cleanup
    - Link management: accepts_links get/set, callback setters
    - Proof strategy: set_proof_strategy with validation
    - Request handlers: register_request_handler, deregister_request_handler with path hashing
    - Receive: receive(packet) with LINKREQUEST handling, decryption, packet callback dispatch
    - GROUP keys: create_keys, get_private_key, load_private_key, load_public_key
      using Token (Fernet-like) for GROUP encryption
    - Encryption: PLAIN passthrough, SINGLE via Identity with ratchet support,
      GROUP via Token
    - Decryption: PLAIN passthrough, SINGLE with ratchet retry and reload from storage,
      GROUP via Token
    - Signing: sign(message) via Identity for SINGLE destinations
    - Default app data: set_default_app_data (bytes or callable), clear_default_app_data
    - Ratchet support: enable_ratchets, enforce_ratchets, set_retained_ratchets,
      set_ratchet_interval, rotate_ratchets, ratchet file persistence with
      MessagePack serialization and signature verification
    - String representation: to_s with <name:hexhash> format
  - Added RNS::TypeError custom exception class
  - Updated src/rns/transport.cr — added register_destination, deregister_destination,
    destinations accessor, clear_destinations helper
  - Updated src/rns/packet.cr — added Packet#send stub, updated type casts to handle
    both Destination and Destination::Stub in pack() and PacketReceipt.validate_proof()
  - Created spec/rns/destination_spec.cr (124 examples)
    - Constants: all type, direction, proof strategy, request policy, ratchet constants
    - Static methods: expand_name (with/without identity, dots rejection, splat),
      hash (truncated hash size, different names/identities, nil identity, raw bytes,
      invalid bytes, computation verification), app_and_aspects_from_name,
      hash_from_name_and_identity
    - Constructor: SINGLE IN auto-identity, SINGLE OUT with identity, OUT without identity
      rejection, PLAIN without/with identity, GROUP, invalid type/direction, dots rejection,
      default properties, Transport registration, register: false
    - Name and hash: includes aspects, hexhash appears twice for SINGLE IN,
      hexhash matches hash, same identity same hash, different hashes, name_hash size
    - String representation: includes name and hexhash
    - Callbacks: link_established, packet, proof_requested
    - Proof strategy: set PROVE_NONE/ALL/APP, unsupported rejection
    - Accepts links: default true, set false, set back true
    - Request handlers: register, empty path rejection, invalid policy rejection,
      deregister, deregister non-existent, multiple handlers
    - GROUP keys: create, get_private_key, load_private_key, PLAIN/SINGLE raises,
      load_public_key raises
    - Encryption: PLAIN passthrough, SINGLE encrypt/decrypt roundtrip, overhead check,
      random IV, wrong identity, empty plaintext, 100 random roundtrips,
      GROUP roundtrip, GROUP without key raises, shared GROUP key, 50 GROUP roundtrips
    - Signing: SINGLE sign, verify by identity, PLAIN returns nil, GROUP returns nil
    - Default app data: set bytes, set callable, clear
    - Announce: creates packet, data contains public key, data contains name_hash,
      correct structure size, includes app_data, uses default app_data, callable app_data,
      PATH_RESPONSE context, GROUP rejection, OUT rejection, signature validation,
      two announces differ
    - Ratchets: enable creates file, rotate adds ratchet, respects interval,
      enforce with/without ratchets, set_retained_ratchets, set_ratchet_interval,
      rotate without ratchets raises, ratchet file persistence and reload
    - Receive: packet callback for DATA, decryption failure returns false,
      PLAIN receives plaintext
    - Transport integration: register, deregister, multiple destinations
    - DestinationInterface compliance: includes interface, usable as interface
    - Stress tests: 50 destination creation, 20 announce generation
  - Ran crystal spec — 525 examples, 0 failures

2026-03-10 — Task 4.1: Transport core and data structures
  - Expanded src/rns/transport.cr from stub to full Transport module
    - Transport type constants: BROADCAST (0x00), TRANSPORT (0x01), RELAY (0x02), TUNNEL (0x03)
    - Reachability constants: REACHABILITY_UNREACHABLE/DIRECT/TRANSPORT
    - Pathfinder constants: PATHFINDER_M (128), PATHFINDER_R (1), PATHFINDER_G (5),
      PATHFINDER_RW (0.5), PATHFINDER_E (604800), AP_PATH_TIME (86400), ROAMING_PATH_TIME (21600)
    - Path request constants: PATH_REQUEST_TIMEOUT (15), PATH_REQUEST_GRACE (0.4),
      PATH_REQUEST_RG (1.5), PATH_REQUEST_MI (20)
    - State constants: STATE_UNKNOWN (0x00), STATE_UNRESPONSIVE (0x01), STATE_RESPONSIVE (0x02)
    - Timeout/limit constants: REVERSE_TIMEOUT (480), DESTINATION_TIMEOUT (604800),
      MAX_RECEIPTS (1024), MAX_RATE_TIMESTAMPS (16), PERSIST_RANDOM_BLOBS (32),
      MAX_RANDOM_BLOBS (64), HASHLIST_MAXSIZE (1000000), MAX_PR_TAGS (32000)
    - Job interval constants matching Python exactly
    - All table index constants: IDX_PT_*, IDX_RT_*, IDX_AT_*, IDX_LT_*, IDX_TT_*
    - Record types: PathEntry, AnnounceEntry, ReverseEntry, LinkEntry, TunnelEntry, AnnounceRateEntry
    - Core state: @@interfaces, @@destinations, @@pending_links, @@active_links,
      @@packet_hashlist, @@receipts, all table hashes, timing state, traffic counters,
      identity, locking (Mutex), save operation guards
    - AnnounceHandler abstract module
    - Registration: register_destination (with duplicate check for IN destinations, sets MTU),
      deregister_destination, register_announce_handler, deregister_announce_handler
    - Full accessor methods for all state
    - reset() method for test isolation
  - Created src/rns/transport/path_management.cr
    - Path lookup: has_path, hops_to, next_hop, next_hop_interface, first_hop_timeout
    - Path state: expire_path (sets timestamp to 0, triggers culling),
      mark_path_unresponsive, mark_path_responsive, mark_path_unknown_state, path_is_unresponsive
    - Path mutation: update_path (insert/update entries), remove_path (with state cleanup)
    - Path request: request_path (constructs path request packet, records timestamp)
    - Interface management: register_interface, deregister_interface, find_interface_from_hash
    - Persistence: save_path_table (MessagePack serialization with PERSIST_RANDOM_BLOBS limit),
      load_path_table (with hop increment on load, robust integer type handling),
      save_packet_hashlist, load_packet_hashlist, save_tunnel_table, persist_data
    - MessagePack type conversion helpers: msgpack_to_i64, msgpack_to_f64
  - Updated src/rns.cr to require transport/path_management
  - Created spec/rns/transport_spec.cr (120 examples)
    - Constants: transport types (5), reachability (3), pathfinder (7), path request (4),
      state (3), timeout/limit (9), job interval (6), table indices (5), APP_NAME
    - Initial state: all tables empty, counters zero, no identity, jobs not running
    - Registration: register/deregister destinations, duplicate IN rejection, MTU setting,
      OUT with same hash allowed, register/deregister interfaces, find_interface_from_hash
    - Path management: has_path, hops_to (unknown/known/direct), next_hop,
      next_hop_interface, first_hop_timeout, expire_path (preserves fields, resets culling),
      mark_path_unresponsive/responsive/unknown, path_is_unresponsive, update_path (create/overwrite),
      remove_path (with state cleanup), request_path (timestamp recording, custom tag)
    - Persistence: save/load path table roundtrip (2 entries with blobs and interfaces),
      load from nonexistent file, PERSIST_RANDOM_BLOBS limit enforcement,
      save/load packet hashlist roundtrip, tunnel table save with paths, persist_data
    - Record types: PathEntry, AnnounceEntry, ReverseEntry, LinkEntry, TunnelEntry, AnnounceRateEntry
    - AnnounceHandler: register/deregister, multiple handlers
    - Reset: clears all state
    - Stress tests: 100 path entries, 50 destination registrations,
      20-entry persistence roundtrip
  - Ran crystal spec — 645 examples, 0 failures

2026-03-10 — Task 4.2: Transport announce handling
  - Created src/rns/transport/announce_handler.cr porting announce logic from RNS/Transport.py
    - timebase_from_random_blob: extracts 40-bit big-endian emission timestamp from random blob bytes 5..9
    - timebase_from_random_blobs: returns maximum timebase across array of random blobs
    - announce_emitted: extracts emission timebase from announce packet data
    - add_packet_hash / packet_hash_in_list?: packet deduplication hashlist management
    - packet_filter: full packet filtering — transport ID checks, context passthrough (KEEPALIVE,
      RESOURCE, CACHE_REQUEST, CHANNEL), PLAIN/GROUP hop limit enforcement, PLAIN/GROUP announce
      rejection, hash deduplication with SINGLE announce bypass
    - inbound_announce: core announce processing — signature validation via Identity.validate_announce,
      local destination rejection, transport_id handling (rebroadcast detection, local_rebroadcasts
      increment, passed-on detection), path table insertion with random blob deduplication and
      timebase comparison, expired path replacement, unresponsive path replacement, hop count
      comparison, announce table entry creation for retransmission, rate limiting
    - check_announce_rate: per-destination rate limiting with timestamps, violations, blocking
    - invoke_announce_handlers: calls registered AnnounceHandler callbacks with aspect_filter
      matching, PATH_RESPONSE exclusion, fiber-spawned execution
    - process_announce_table: periodic retransmission processing — retry limit enforcement,
      local rebroadcast max enforcement, retransmit timeout checking, new packet creation
      with HEADER_2/TRANSPORT transport, held announce reinsertion
    - mark_path_unknown_for_destination: convenience wrapper for mark_path_unknown_state
    - clean_announce_cache: removes cached announce files not in path/tunnel tables
    - cache_packet: writes packet to disk with MessagePack serialization
  - Added Identity.validate_announce to src/rns/identity.cr
    - Full announce validation: public key extraction, ratchet detection via context_flag,
      name_hash/random_hash/signature/app_data parsing, signed_data reconstruction,
      signature verification via Ed25519, destination hash verification (name_hash + identity hash),
      known public key mismatch detection, remember/recall integration, ratchet remembering
    - Supports only_validate_signature mode for quick signature-only checks
    - Blackholed identity filtering
  - Changed Destination hash/hexhash from getter to property (needed for announce rebroadcast)
  - Fixed Packet.pack to set destination_type from destination (was only set during unpack)
  - Created spec/rns/transport/announce_handler_spec.cr (69 examples)
    - Timebase: random blob extraction (normal, large, short, zero, big-endian), blob array max,
      single blob, announce emission extraction
    - Packet filter: KEEPALIVE/RESOURCE/CACHE_REQUEST/CHANNEL passthrough, PLAIN hop limit,
      PLAIN/GROUP announce rejection, GROUP hop limit, unseen SINGLE packets, SINGLE announce
      bypass hashlist, non-announce hash dedup
    - Packet hash: add/check, unknown hash returns false
    - Identity.validate_announce: valid packet, signature-only mode, remembers destination,
      doesn't remember on sig-only, non-announce rejection, tampered data, app_data validation,
      app_data recall, public key mismatch rejection, 20-packet stress test
    - Inbound announce: path table addition, correct hops, local destination rejection,
      invalid signature rejection, announce table addition, PATH_RESPONSE not in announce table,
      random blob storage, received_from without transport_id, deduplication, fewer hops
      with newer emission, more hops rejection, handler callbacks, aspect_filter filtering,
      PATH_RESPONSE handler exclusion, app_data handling, non-announce rejection, max hops rejection
    - Rate limiting: first announce not blocked, rate entry creation, timestamp updates,
      MAX_RATE_TIMESTAMPS enforcement
    - Process announce table: empty returns empty, retry limit removal, local rebroadcast max
      removal, timeout retransmission, future timeout no retransmit, PATH_RESPONSE context
      for block_rebroadcasts, held announce reinsertion
    - mark_path_unknown_for_destination: clears unresponsive state
    - Cache: packet caching to file, no cache without force, cache cleanup removes orphans,
      cache cleanup preserves active paths
    - Stress: 50 different announces, 20 processing cycles drain all, 30 rapid rate checks
  - Ran crystal spec — 714 examples, 0 failures

2026-03-10 — Task 4.3: Transport packet routing and delivery
  - Created src/rns/link_like.cr with LinkLike abstract module and LinkStub test implementation
    - Constants: PENDING, HANDSHAKE, ACTIVE, STALE, CLOSED, STALE_TIME, ESTABLISHMENT_TIMEOUT_PER_HOP
    - Abstract methods: link_id, initiator?, status, status=, destination_hash, expected_hops, attached_interface
  - Updated src/rns/transport.cr with full packet routing implementation:
    - Link management: register_link, activate_link, find_link_for_destination, find_best_link
    - Transmit: records to transmit_log for test verification, updates traffic_txb
    - Outbound: full routing via path_table (multi-hop HEADER_2, direct HEADER_1), broadcast fallback
    - Inbound: IFAC stub, unpack, hop increment, packet_filter, plain broadcast, transport forwarding,
      link transport handling, announce processing, link request delivery, proof handling
    - Jobs loop: link cleanup, receipt culling, table culling, blackhole expiry, hashlist management
    - Helpers: mark_packet_sent, cull_tables, cull_blackholes, is_local_client_interface, from_local_client
  - Extended src/rns/transport/tunnel_management.cr:
    - handle_tunnel, void_tunnel_interface, tunnel_synthesize_handler, synthesize_tunnel_data
  - Created spec/rns/transport/packet_routing_spec.cr with 76 test examples covering:
    - LinkLike constants and LinkStub, link management, transmit, outbound routing, inbound pipeline,
      transport forwarding, tunnel management, jobs loop, proof routing, transport flags, stress tests
  - Ran crystal spec — 790 examples, 0 failures

2026-03-10 — Task 4.4: Transport caching and persistence
  - Added load_tunnel_table() to src/rns/transport/path_management.cr
    - Deserializes tunnel entries from MessagePack format matching save_tunnel_table
    - Reconstructs TunnelEntry records with nested PathEntry paths
    - Increments hop count on load (matching Python behavior for cached packets)
    - Skips tunnels with no valid paths, robust error handling per-entry
  - Added caching methods to src/rns/transport.cr:
    - should_cache(packet): returns false (disabled per Python TODO), placeholder for future
    - cache(packet, force_cache, packet_type): writes packet raw+interface_ref as MessagePack
      to cache_path/ or cache_path/announces/ based on packet_type
    - get_cached_packet(packet_hash, packet_type): reads cached packet from disk, returns Packet
    - cache_request_packet(packet): handles CACHE_REQUEST by replaying cached packet to inbound
    - cache_request(packet_hash, destination): local cache lookup or network request
    - clean_cache(): delegates to clean_announce_cache, updates timestamp
  - Added OwnerRef record type to Transport module:
    - Lightweight record (is_connected_to_shared_instance, storage_path, cache_path,
      transport_enabled) avoiding circular dependency with Reticulum class
    - owner= setter, owner accessor updated
  - Added start() initialization method:
    - Creates or loads transport identity from storage_path/transport_identity
    - Loads packet hashlist unless connected to shared instance
    - Creates control destinations (path request, tunnel synthesize)
    - Loads path table and tunnel table when transport_enabled
    - Sets start_time, deferred cache cleaning (60s), deferred management announces (15s)
    - Starts periodic job loop fiber
  - Added job loop lifecycle:
    - start_job_loop(): spawns fiber running jobs() at JOB_INTERVAL (250ms)
    - stop_job_loop(): sets flag to stop fiber gracefully
    - job_loop_running? accessor, @@job_fiber and @@job_loop_running state
  - Added exit_handler():
    - Stops job loop, calls persist_data() to save all tables
    - No-op for shared instance connections or when owner not set
  - Updated reset() to clear new state (job_loop_running, job_fiber)
  - Created spec/rns/transport/caching_persistence_spec.cr (53 examples)
    - OwnerRef: default values, custom values, set/get on Transport, nil default
    - should_cache: always false for any packet type
    - cache/get_cached_packet: force_cache writes file, no cache without force,
      announce subdirectory, no-op without owner, retrieval roundtrip, announce retrieval,
      non-existent returns nil, no-owner returns nil, 10-packet random roundtrip
    - cache_request_packet: wrong size returns false, uncached returns false
    - clean_cache: removes orphans, preserves active paths, no-op without owner
    - Packet hashlist persistence: save/load roundtrip, missing file, empty list, concurrent saves
    - Path table persistence: save/load roundtrip, missing file, random blob limit
    - Tunnel table persistence: save/load roundtrip, missing file, hop increment,
      empty paths skipped, multiple tunnels
    - persist_data: saves all three tables
    - start(): identity creation, identity loading, owner ref, start_time, control destinations,
      job loop, hashlist loading, transport-enabled path/tunnel loading, transport-disabled skip,
      existing identity preservation
    - Job loop: start/stop cleanly, no double-start
    - exit_handler: persists data and stops loop, no-op without owner, no-op for shared instance
    - Full lifecycle: start -> populate -> exit -> restart preserves state
    - Stress: 50 path entries, 100 packet hashes, 20 cached packets, 10 tunnels × 5 paths
  - Ran crystal spec — 843 examples, 0 failures

2026-03-10 — Task 5.1: Channel module
  - Created src/rns/channel.cr porting RNS/Channel.py (705 LOC)
    - SystemMessageTypes module: SMT_STREAM_DATA (0xff00)
    - CEType module: ME_NO_MSG_TYPE (0), ME_INVALID_MSG_TYPE (1), ME_NOT_REGISTERED (2),
      ME_LINK_NOT_READY (3), ME_ALREADY_SENT (4), ME_TOO_BIG (5)
    - ChannelException class: custom Exception with type code
    - MessageState module: MSGSTATE_NEW (0), MSGSTATE_SENT (1), MSGSTATE_DELIVERED (2),
      MSGSTATE_FAILED (3)
    - MessageBase abstract class: abstract pack/unpack, class_getter msgtype pattern
    - ChannelOutletBase(TPacket) abstract generic class: send, resend, mdu, rtt, is_usable,
      get_packet_state, timed_out, set_packet_timeout_callback, set_packet_delivered_callback,
      get_packet_id — matching Python ChannelOutletBase(ABC, Generic[TPacket])
    - Envelope(TPacket) class: message wrapping with 6-byte header (msgtype + sequence + length),
      pack/unpack with big-endian encoding, state tracking (tries, unpacked, packed, tracked)
    - Channel(TPacket) generic class with full window management:
      - Window constants: WINDOW (2), WINDOW_MIN (2), WINDOW_MAX_SLOW (5), WINDOW_MAX_MEDIUM (12),
        WINDOW_MAX_FAST (48), WINDOW_MAX = WINDOW_MAX_FAST, WINDOW_FLEXIBILITY (4)
      - Window limit constants: WINDOW_MIN_LIMIT_SLOW (2), WINDOW_MIN_LIMIT_MEDIUM (5),
        WINDOW_MIN_LIMIT_FAST (16)
      - Rate constants: FAST_RATE_THRESHOLD (10), RTT_FAST (0.18), RTT_MEDIUM (0.75), RTT_SLOW (1.45)
      - Sequence constants: SEQ_MAX (0xFFFF), SEQ_MODULUS (0x10000)
      - Constructor: window initialization based on RTT (slow links get window=1)
      - register_message_type: validates MSGTYPE exists, < 0xf000, supports system types
      - add_message_handler/remove_message_handler: callback management with dedup
      - mdu: outlet.mdu - 6, capped at 0xFFFF
      - is_ready_to_send?: checks outlet usability and outstanding window count
      - send: creates envelope, packs, sends through outlet, sets up delivery/timeout callbacks
      - _receive: unpacks envelope, validates sequence, handles out-of-order with rx_ring,
        delivers contiguous messages in order, runs callbacks
      - Delivery handling: removes from tx_ring, increases window, adapts rate
      - Timeout handling: retries with resend, decreases window, decreases window_max,
        tears down after max_tries (5)
      - Rate adaptation: tracks fast/medium_rate_rounds, upgrades window_max/window_min
        after sustained FAST_RATE_THRESHOLD rounds at fast or medium RTT
      - Sequence wrapping at SEQ_MODULUS (0x10000)
      - Reentrant mutex (matching Python's RLock) for thread safety
      - shutdown: clears callbacks and rings
    - Note: LinkChannelOutlet deferred to Task 5.2/5.3 when Link module is available
  - Updated src/rns.cr to require channel module
  - Created spec/rns/channel_spec.cr (91 examples)
    - Constants: SystemMessageTypes (1), CEType (6), MessageState (4), Channel window (16)
    - ChannelException: type code storage, message storage, Exception subclass
    - MessageBase: MSGTYPE getter, no-arg construction, pack/unpack, roundtrip
    - Envelope: initialization (message, raw), pack header encoding (big-endian),
      unpack message reconstruction, unregistered MSGTYPE rejection, pack/unpack roundtrip
    - Channel constructor: normal RTT, slow RTT (window=1), rate round initialization
    - register_message_type: valid type, multiple types, system-reserved rejection,
      nil MSGTYPE rejection
    - Message handlers: add, no duplicates, remove, remove non-existent
    - MDU: header overhead, cap at 0xFFFF, small MDU
    - is_ready_to_send?: usable outlet, unusable outlet, full window
    - send: message send, sequence increment, not-ready rejection, too-big rejection,
      callback setup, raw bytes through outlet
    - _receive: dispatch to handler, in-order delivery of out-of-order messages,
      duplicate rejection, handler chain (stop on true, continue on false),
      unregistered type graceful handling
    - Delivery: tx_ring removal, window increase
    - Timeout: retry with resend, window decrease, window_min floor
    - Rate adaptation: fast_rate_rounds increment/reset, medium_rate_rounds increment/reset,
      WINDOW_MAX_FAST upgrade, WINDOW_MAX_MEDIUM upgrade
    - shutdown: clears callbacks and rings
    - Sequence wrapping: wraps at SEQ_MODULUS
    - Timeout computation: formula verification, increasing with tries
    - Send/receive roundtrip: TestMessage, AnotherMessage
    - Stress: 50 messages with delivery ack, 30 in-order receives, 20 random roundtrips
  - Ran crystal spec — 934 examples, 0 failures

2026-03-10 — Task 5.2: Link module — establishment and encryption
  - Created src/rns/link.cr (~800 LOC) with full Link class implementation
  - Link class implements both LinkLike (Transport integration) and DestinationInterface (Packet creation)
  - All constants matching Python RNS.Link exactly: ECPUBSIZE=64, KEYSIZE=32, modes (AES256_CBC, AES256_GCM),
    states (PENDING, HANDSHAKE, ACTIVE, STALE, CLOSED), timeouts, keepalive intervals, stale times
  - 3-step ECDH handshake: initiator sends X25519+Ed25519 pub keys + signalling bytes,
    responder validates/derives shared secret/sends proof, initiator verifies proof
  - Key derivation via HKDF from shared secret → derived_key for encrypt/decrypt
  - encrypt_data/decrypt_data using Identity.encrypt/decrypt with derived key + token
  - sign/validate using Ed25519 with link-specific key derivation
  - identify(identity) sends signed identification over established link
  - request(path, data, ...) with MessagePack serialization and RequestReceipt tracking
  - Static helpers: signalling_bytes, mtu_from_lr_packet, mtu_from_lp_packet, mode_from_lr_packet,
    mode_from_lp_packet, link_id_from_lr_packet, validate_request
  - LinkCallbacks class with 7 callback properties (established, closed, packet, resource_started, etc.)
  - RequestReceipt class with states (FAILED, SENT, DELIVERED, RECEIVING, READY) and callbacks
  - RequestReceiptCallbacks class matching Python interface
  - LinkChannelOutlet class extending ChannelOutletBase(Packet) — deferred integration due to Crystal
    compiler codegen bug with non-generic class inheriting from generic instantiation
  - Watchdog fiber for link timeout management (keepalive, stale detection, establishment timeout)
  - MTU signalling bytes encoding (3 bytes: mode in upper bits, MTU in lower 21 bits)
  - Fixed LinkLike::ECPUBSIZE from 32*8 (256 bits, wrong) to 32+32 (64 bytes, matching Python)
  - Fixed Transport.cr ecpub_half calculation (removed extra //8 since ECPUBSIZE is now bytes)
  - Updated src/rns.cr to require link module
  - Created spec/rns/link_spec.cr with 89 tests (84 passing, 5 pending for Channel outlet):
    - Constants verification (11 tests)
    - Signalling bytes encoding/decoding (4 tests)
    - Responder construction (3 tests)
    - ECDH handshake with simulated key exchange (4 tests)
    - Encrypt/decrypt roundtrip with derived keys (6 tests)
    - Sign/validate (1 test)
    - State transitions and teardown (5 tests)
    - Timing helpers: inactive_for, no_inbound_for, etc. (5 tests)
    - Keepalive tracking (4 tests)
    - Callbacks (4 tests)
    - Resource strategy (3 tests)
    - Phy stats tracking (2 tests)
    - Status-gated getters (4 tests)
    - DestinationInterface compliance (2 tests)
    - LinkLike compliance (2 tests)
    - link_id_from_lr_packet (2 tests)
    - MTU/mode extraction from packets (4 tests)
    - validate_request (3 tests)
    - Full handshake simulation (1 test)
    - identify method (2 tests)
    - update_mdu (1 test)
    - to_s, get_mode, get_remote_identity (3 tests)
    - LinkChannelOutlet (5 pending due to Crystal codegen bug)
    - RequestReceipt (6 tests)
    - RequestReceiptCallbacks (1 test)
    - LinkCallbacks (1 test)
    - Stress tests: 100 encrypt/decrypt cycles, concurrent link creation (2 tests)
  - Ran crystal spec — 1023 examples, 0 failures, 5 pending

2026-03-11 — Task 5.3: Link module — lifecycle management
  - Completed lifecycle management for src/rns/link.cr
  - send() convenience method: creates and sends packet over link, tracks tx/txbytes
  - Resource management methods (stubs ready for Task 5.4):
    - register_outgoing_resource, register_incoming_resource (hash-based tracking)
    - cancel_outgoing_resource, cancel_incoming_resource
    - has_incoming_resource?, ready_for_new_resource?
    - resource_concluded: updates expected_rate from transfer size/time,
      tracks last_resource_window and last_resource_eifr
    - get_last_resource_window, get_last_resource_eifr
  - link_closed: clears incoming/outgoing resource lists on teardown
  - Resource callback setters: set_resource_callback, set_resource_started_callback,
    set_resource_concluded_callback with proper type signatures
  - Complete handle_request dispatch: wires to Destination request_handlers,
    validates ALLOW_NONE/ALLOW_ALL/ALLOW_LIST policies, invokes response_generator,
    packs and sends response via MessagePack, logs disallowed requests
  - RequestReceipt enhancements:
    - request_resource_concluded: transitions DELIVERED on success, FAILED on failure,
      spawns response timeout fiber
    - response_timeout_job: polls for timeout on resource-based responses
    - response_resource_progress: updates RECEIVING status, marks packet_receipt
      delivered, calls progress callback
    - @resource_response_timeout instance variable for timeout tracking
  - Made set_destination and establishment_timeout= public for test access
  - Fixed MessagePack::Any#as_bytes -> .raw.as(Bytes) for Crystal compatibility
  - Fixed closure capture for nilable plaintext in receive dispatch (cb.call)
  - Updated LinkCallbacks resource types to proper signatures
  - Updated outgoing_resources/incoming_resources from Array(Nil) to Array(Bytes)
  - Updated spec/rns/link_spec.cr with 71 new test examples (155 passing, 5 pending):
    - send(): packet creation, tx/txbytes tracking, CLOSED nil return,
      last_outbound update, packet_type/context params (5 tests)
    - Resource management: register/track outgoing, register/track incoming,
      has_incoming_resource? false for unknown, cancel outgoing, cancel incoming,
      ready_for_new_resource?, resource_concluded incoming, resource_concluded outgoing,
      clears on link_closed, last window/eifr nil initially (10 tests)
    - Resource callbacks: set_resource_callback, set_resource_started_callback,
      set_resource_concluded_callback (3 tests)
    - Keepalive timing: formula verification, stale_time tracking, medium RTT scaling,
      send_keepalive marks last_keepalive, send_keepalive doesn't update last_data,
      had_outbound is_keepalive true/false (7 tests)
    - Stale detection: STALE_FACTOR=2, STALE_TIME formula, default 720s,
      stale adjusts with RTT, STALE->ACTIVE on inbound, inactive_for,
      no_data_for (7 tests)
    - Teardown: CLOSED status, DESTINATION_CLOSED for responder,
      INITIATOR_CLOSED for initiator, purges crypto keys, clears resources,
      link_closed callback, teardown_packet matching/non-matching,
      reverse teardown reason, no teardown packet if PENDING (9 tests)
    - RTT computation: nil initially, keepalive from RTT, establishment rate,
      RTT range effect on keepalive, rtt property (5 tests)
    - Receive dispatch: ignores CLOSED, increments rx/rxbytes, updates last_inbound,
      STALE->ACTIVE, keepalive doesn't update last_data, non-keepalive updates last_data,
      LINKCLOSE teardown, responder keepalive response, initiator ignores own keepalive (9 tests)
    - handle_request: ALLOW_ALL dispatch, ALLOW_NONE rejection, no-op when not ACTIVE,
      no-op for unregistered path (4 tests)
    - RequestReceipt resource: DELIVERED on success, FAILED on failure,
      response_resource_progress updates status/progress, ignores when FAILED,
      marks packet_receipt delivered (5 tests)
    - Watchdog: PENDING timeout, HANDSHAKE timeout (2 tests)
    - prove_packet: creates proof with signature (1 test)
    - Stress: 50 resource register/conclude cycles, concurrent link creation (2 tests)
  - Ran crystal spec — 1094 examples, 0 failures, 5 pending

2026-03-11 — Task 5.4: Resource module
  - Created src/rns/vendor/bz2.cr — BZip2 FFI wrapper for wire-compatible compression
    - Links against system libbz2 via @[Link("bz2")]
    - RNS::BZip2.compress(data) with block_size_100k=9 matching Python bz2.compress default
    - RNS::BZip2.decompress(data) with auto-growing buffer (doubles on BZ_OUTBUFF_FULL, max 256MB)
  - Created src/rns/resource.cr (~900 LOC) porting RNS/Resource.py (1361 LOC)
    - Resource class with all constants matching Python exactly:
      - Window: WINDOW=4, WINDOW_MIN=2, WINDOW_MAX_SLOW=10, WINDOW_MAX_VERY_SLOW=4, WINDOW_MAX_FAST=75
      - Rate: RATE_FAST=6250, RATE_VERY_SLOW=250
      - Size: SDU=Packet::MDU, MAPHASH_LEN=4, RANDOM_HASH_SIZE=4, MAX_EFFICIENT_SIZE=1048575
      - Timeout: PART_TIMEOUT_FACTOR=4, PART_TIMEOUT_FACTOR_AFTER_RTT=2, PROOF_TIMEOUT_FACTOR=3,
        MAX_RETRIES=16, MAX_ADV_RETRIES=4, SENDER_GRACE_TIME=10.0
      - Status: STATUS_NONE through CORRUPT (0x00-0x08)
    - Sender construction: data → metadata combine → bz2 compress → random_hash prefix →
      link.encrypt_data → split into SDU-sized parts with hashmap collision guard
    - Receiver via Resource.accept() static method and new_receiver protected constructor
    - Full lifecycle methods: advertise, watchdog_job, request_next, request, receive_part,
      assemble, prove, validate_proof, cancel, _rejected, get_progress, update_eifr
    - Adaptive windowing: FAST/SLOW/VERY_SLOW rate adaptation based on RTT measurements
    - Watchdog fiber for timeout management (replaces Python threading.Thread)
    - Window management with flexibility scaling
    - Hashmap-based part verification with collision detection
  - ResourceAdvertisement class with pack/unpack using MessagePack
    - Flag encoding (6 bits: encrypted, compressed, split, is_request, is_response, has_metadata)
    - HASHMAP_MAX_LEN computed from Link::MDU and advertisement overhead
    - Constants: OVERHEAD=108, TRANSFER_OVERHEAD=4
  - Updated src/rns/reticulum.cr — added class-level path constants:
    - @@configdir, @@storagepath, @@cachepath, @@resourcepath with getters/setters
  - Updated src/rns.cr — added requires for vendor/bz2 and resource modules
  - Updated src/rns/transport.cr — changed cache_request parameter type from
    Destination to Destination::DestinationInterface (allows Link objects)
  - Created spec/rns/resource_spec.cr (77 examples)
    - BZip2: empty, small, large, repetitive data, 50 random roundtrips
    - Resource constants: window (9), rate (2), size (6), timeout (10), hashmap (2), status (9)
    - Sender construction: 13 tests — creation, hashes, compression, parts, SDU, timeout, request_id
    - get_map_hash: size, uniqueness, determinism
    - Progress: initial, complete, partial, segment, transfer_size, data_size, parts, segments
    - Cancel: status, callback, already-complete
    - _rejected: initiator, non-initiator
    - Callbacks: progress, concluded
    - Window management: initial_window, window_max, flexibility
    - validate_proof: correct, incorrect, failed, callback
    - EIFR: initial, update, update_eifr
    - hashmap_update: updates height, maps parts
    - Stress: 20 resources, 10 proof validations
    - ResourceAdvertisement: constants (3), pack/unpack (6), flag encoding (5), stress (1)
    - Reticulum paths: configdir, storagepath, resourcepath
    - to_s representation
  - Ran crystal spec — 1171 examples, 0 failures, 5 pending

2026-03-11 — Task 6.1: Buffer module
  - Created src/rns/buffer.cr (~270 LOC) porting RNS/Buffer.py (369 LOC)
  - StreamDataMessage class (extends MessageBase):
    - MSGTYPE = 0xff00 (SMT_STREAM_DATA, system-reserved)
    - STREAM_ID_MAX = 0x3fff (14-bit stream id)
    - OVERHEAD = 8 (2 stream header + 6 channel envelope)
    - MAX_DATA_LEN = Link::MDU - OVERHEAD
    - 2-byte big-endian header: bits 15=eof, 14=compressed, 13..0=stream_id
    - pack/unpack with BZip2 decompression on receive when compressed flag set
    - Validates stream_id range (0-16383)
  - RawChannelReader(TPacket) generic class:
    - Registers StreamDataMessage as system message type on channel
    - Receives data via channel message handler, filters by stream_id
    - Internal IO::Memory buffer with compact-on-read for memory efficiency
    - read(size) returns Bytes?, nil when empty and not EOF, empty Bytes at EOF
    - readinto(buffer) matching Python readinto API
    - available() returns buffered byte count
    - eof?() tracks end-of-stream flag
    - Ready callbacks: add_ready_callback, remove_ready_callback
    - Callbacks spawned in fibers (matching Python's threading.Thread)
    - close() removes handler and clears listeners
    - readable?/writable?/seekable?/closed? capability queries
  - RawChannelWriter(TPacket) generic class:
    - MAX_CHUNK_LEN = 16384 (16 KiB), COMPRESSION_TRIES = 4
    - write(data) with adaptive BZip2 compression:
      - Tries compression at full chunk, then half, then third
      - Uses compressed version if smaller than both MAX_DATA_LEN and original
      - Falls back to uncompressed if compression doesn't help
    - Caps single write at MAX_CHUNK_LEN
    - Returns 0 when channel not ready (catches ME_LINK_NOT_READY)
    - close() waits for channel readiness then sends EOF-flagged empty message
    - writable?/readable?/seekable? capability queries
  - Buffer module with factory methods:
    - create_reader(stream_id, channel, ready_callback?) -> RawChannelReader
    - create_writer(stream_id, channel) -> RawChannelWriter
    - create_bidirectional_buffer(rx_id, tx_id, channel, ready_callback?)
      -> {RawChannelReader, RawChannelWriter} tuple
    - All methods generic over TPacket via forall
  - Updated src/rns.cr to require buffer module
  - Created spec/rns/buffer_spec.cr (69 examples)
    - StreamDataMessage constants: STREAM_ID_MAX, OVERHEAD, MAX_DATA_LEN, MSGTYPE (4 tests)
    - Constructor: defaults, stream_id+data, eof, compressed, range validation (7 tests)
    - Pack: nil stream_id rejection, header-only, with data, eof bit, compressed bit,
      both bits, full 14-bit range (7 tests)
    - Unpack: stream_id+data, eof flag, empty data, BZip2 decompression, too-short (5 tests)
    - Pack/unpack roundtrip: flag preservation, 100 random messages (2 tests)
    - RawChannelReader constructor: capabilities (1 test)
    - Receiving: matching stream_id, ignoring other streams, accumulation,
      eof flag, nil on empty, empty at eof, eof-only message (7 tests)
    - readinto: fills buffer, nil on empty, partial reads (3 tests)
    - Partial reads: retain remainder, byte-by-byte (2 tests)
    - Ready callbacks: invocation, removal, multiple callbacks, cumulative count (4 tests)
    - Close: removes handler and listeners (1 test)
    - RawChannelWriter constants: MAX_CHUNK_LEN, COMPRESSION_TRIES (2 tests)
    - Constructor: capabilities (1 test)
    - Write: send, bytes consumed, MAX_CHUNK_LEN cap, not-ready returns 0,
      packet through outlet, empty write (6 tests)
    - Compression: compressible data, small data skip, incompressible fallback (3 tests)
    - Buffer.create_reader: type check, callback, no callback (3 tests)
    - Buffer.create_writer: type check (1 test)
    - Buffer.create_bidirectional_buffer: tuple type, different stream_ids, callback (3 tests)
    - Integration: loopback roundtrip, sequential writes, eof propagation,
      eof flag parsing (4 tests)
    - Stress: 50 write/delivery cycles, 20 bidirectional creations,
      30 random read/write loopback roundtrips (3 tests)
  - Ran crystal spec — 1240 examples, 0 failures, 5 pending

2026-03-11 — Task 6.2: Resolver module (stub)
  - Created src/rns/resolver.cr porting RNS/Resolver.py (35 LOC)
    - Resolver class with resolve_identity(full_name) static method
    - Returns nil (Identity?) matching Python's pass/None behavior
    - Stub/placeholder for future Reticulum Distributed Identity Resolver system
  - Updated src/rns.cr to require resolver module
  - Created spec/rns/resolver_spec.cr (5 examples)
    - Returns nil for any name, empty name, complex multi-aspect name
    - Correct return type annotation (Identity?)
    - Accepts any string argument
  - Ran crystal spec — 1245 examples, 0 failures, 5 pending

2026-03-11 — Task 7.1: Interface base class
  - Created src/rns/interfaces/interface.cr porting RNS/Interfaces/Interface.py (302 LOC)
    - Abstract Interface class with all properties matching Python exactly
    - Direction flags: IN, OUT, FWD, RPT (false by default)
    - Mode constants: MODE_FULL (0x01), MODE_POINT_TO_POINT (0x02), MODE_ACCESS_POINT (0x03),
      MODE_ROAMING (0x04), MODE_BOUNDARY (0x05), MODE_GATEWAY (0x06)
    - DISCOVER_PATHS_FOR: [MODE_ACCESS_POINT, MODE_GATEWAY, MODE_ROAMING]
    - Ingress control constants: IC_NEW_TIME (7200), IC_BURST_FREQ_NEW (3.5), IC_BURST_FREQ (12),
      IC_BURST_HOLD (60), IC_BURST_PENALTY (300), IC_HELD_RELEASE_INTERVAL (30)
    - Announce constants: IA_FREQ_SAMPLES (6), OA_FREQ_SAMPLES (6), MAX_HELD_ANNOUNCES (256),
      ANNOUNCE_CAP (2), QUEUED_ANNOUNCE_LIFE (86400)
    - MTU flags: AUTOCONFIGURE_MTU (false), FIXED_MTU (false)
    - All properties: name, rxb, txb, online, bitrate, hw_mtu, mode, mtu, detached,
      supports_discovery, discoverable, last_discovery_announce, bootstrap_only,
      parent_interface, spawned_interfaces, tunnel_id
    - Ingress control properties: ingress_control, ic_max_held_announces, ic_burst_hold,
      ic_burst_active, ic_burst_activated, ic_held_release, ic_burst_freq_new, ic_burst_freq,
      ic_new_time, ic_burst_penalty, ic_held_release_interval, held_announces
    - Announce properties: announce_queue, announce_cap, announce_allowed_at,
      announce_rate_target, announce_rate_grace, announce_rate_penalty
    - IFAC properties: ifac_size, ifac_netname, ifac_netkey, ifac_key, ifac_identity, ifac_signature
    - Methods: get_hash(), should_ingress_limit?(), optimise_mtu(), age(), hold_announce(),
      process_held_announces(), received_announce(), sent_announce(),
      incoming_announce_frequency(), outgoing_announce_frequency(), process_announce_queue(),
      final_init(), detach(), process_outgoing() (abstract)
    - HeldAnnounce record: raw, destination_hash, hops, receiving_interface
    - AnnounceQueueEntry record: raw, hops, time
    - Bounded frequency deques with IA_FREQ_SAMPLES/OA_FREQ_SAMPLES max size
    - Announce frequency calculation matching Python algorithm (delta-based averaging)
    - Ingress limiting with burst detection, hold time, and penalty
    - Announce queue processing: stale removal, lowest-hop selection, rate-capped sending
    - Parent interface announce propagation
  - Created RNS::HDLC module in interface.cr (used by Serial, TCP, Pipe, I2P interfaces)
    - Constants: FLAG (0x7E), ESC (0x7D), ESC_MASK (0x20)
    - escape(): replaces FLAG and ESC bytes with escape sequences
    - unescape(): reverses escape sequences back to original bytes
    - frame(): wraps data with FLAG delimiters and escapes contents
  - Updated src/rns.cr to require interfaces/interface module
  - Created spec/rns/interfaces/interface_spec.cr (96 examples)
    - HDLC constants (3), escape (6), unescape (5), roundtrip (2), frame (3)
    - Interface constants: modes (1), DISCOVER_PATHS_FOR (1), freq samples (1),
      MAX_HELD_ANNOUNCES (1), ingress control timing (1), announce cap/life (1),
      MTU flags (1), direction flags (1)
    - Initialization: defaults (1), discovery (1), ingress control (1), held announces (1),
      frequency deques (1), announce queue (1), announce rate (1), IFAC (1), created time (1),
      parent/spawned (1)
    - get_hash: type and size (1), different names (1), consistent hashes (1)
    - age: non-negative (1), increases (1)
    - should_ingress_limit?: disabled (1), below threshold (1), burst activation (1),
      burst mode persistence (1), deactivation after hold time (1)
    - optimise_mtu: base class no-op (1)
    - hold_announce: basic (1), overwrite (1), max capacity (1), overwrite at capacity (1)
    - process_held_announces: empty (1), ingress limiting active (1), releases lowest-hop (1),
      updates ic_held_release (1)
    - received_announce: adds timestamp (1), limits deque (1), propagates to parent (1)
    - sent_announce: adds timestamp (1), limits deque (1), propagates to parent (1)
    - incoming_announce_frequency: no samples (1), one sample (1), positive (1), faster (1)
    - outgoing_announce_frequency: no samples (1), one sample (1), positive (1)
    - process_announce_queue: empty (1), lowest-hop oldest (1), removes sent (1),
      stale removal (1), announce_allowed_at (1), records sent (1), error handling (1)
    - final_init: no-op (1), detach: marks detached (1), to_s: returns name (1)
    - Property mutation: name (1), rxb/txb (1), online (1), bitrate (1), mode (1),
      hw_mtu (1), tunnel_id (1), IFAC (1), announce rate (1), parent/spawned (1)
    - HeldAnnounce record (2), AnnounceQueueEntry record (1)
    - Stress: 100 held announces (1), rapid frequency tracking (1),
      200 HDLC escape/unescape (1), 20 announce queue entries (1)
  - Ran crystal spec — 1341 examples, 0 failures, 5 pending

2026-03-11 — Task 7.2: UDP interface
  - Created src/rns/interfaces/udp_interface.cr porting RNS/Interfaces/UDPInterface.py (140 LOC)
    - UDPInterface class inheriting from Interface base class
    - Constants: BITRATE_GUESS (10_000_000), DEFAULT_IFAC_SIZE (16)
    - Constructor accepts Hash(String, String) configuration with keys:
      name, device, port, listen_ip, listen_port, forward_ip, forward_port
    - "port" serves as default for both listen_port and forward_port (matching Python)
    - Explicit listen_port/forward_port override "port" default
    - HW_MTU set to 1064 matching Python
    - Receive mode: binds UDPSocket with reuse_address, spawns receive fiber
    - Forward mode: creates new UDPSocket per send with broadcast=true
    - process_incoming(data): tracks rxb, invokes inbound callback with data and interface
    - process_outgoing(data): sends via UDP to forward_ip:forward_port, tracks txb,
      logs errors gracefully without raising (matching Python try/except)
    - teardown(): stops receive fiber, closes socket, sets online=false, idempotent
    - to_s: "UDPInterface[name/bind_ip:bind_port]" matching Python __str__
    - Two constructor overloads: with block (for inbound callback) and without
    - dir_in/dir_out properties (IN=true, OUT=false by default)
  - Updated src/rns.cr to require interfaces/udp_interface
  - Created spec/rns/interfaces/udp_interface_spec.cr (28 examples)
    - Constants: BITRATE_GUESS, DEFAULT_IFAC_SIZE (2 tests)
    - Constructor: receive-only, forward-only, bidirectional, port default,
      port override, direction flags (6 tests)
    - process_outgoing: UDP send to receiver, txb tracking, broadcast enabled,
      error handling graceful (4 tests)
    - process_incoming: receive callback with rxb, multiple packets (2 tests)
    - Integration: send/receive between two interfaces, bidirectional communication (2 tests)
    - to_s: formatted string, forward-only format (2 tests)
    - Interface base class: inheritance, counters, HW_MTU, hash (4 tests)
    - Teardown: closes socket and frees port, idempotent (2 tests)
    - Configuration: string hash, missing optional fields (2 tests)
    - Stress: 50 rapid sends, 20 rapid send/receive cycles (2 tests)
  - Ran crystal spec — 1369 examples, 0 failures, 5 pending

2026-03-11 — Task 7.3: TCP interface (client and server)
  - Added RNS::KISS module to src/rns/interfaces/interface.cr
    - Constants: FEND (0xC0), FESC (0xDB), TFEND (0xDC), TFESC (0xDD), CMD_DATA (0x00), CMD_UNKNOWN (0xFE)
    - escape(): replaces FEND and FESC bytes with escape sequences
    - unescape(): reverses escape sequences back to original bytes
    - frame(): wraps data with FEND + CMD_DATA + escaped(data) + FEND
  - Created src/rns/interfaces/tcp_interface.cr porting RNS/Interfaces/TCPInterface.py (661 LOC)
  - TCPInterfaceConstants module: HW_MTU = 262144 shared by client and server
  - TCPClientInterface class inheriting from Interface:
    - Constants: BITRATE_GUESS (10_000_000), DEFAULT_IFAC_SIZE (16), AUTOCONFIGURE_MTU (true),
      RECONNECT_WAIT (5), RECONNECT_MAX_TRIES (nil), INITIAL_CONNECT_TIMEOUT (5),
      TCP keepalive params (USER_TIMEOUT=24, PROBE_AFTER=5, PROBE_INTERVAL=2, PROBES=12),
      I2P keepalive params (USER_TIMEOUT=45, PROBE_AFTER=10, PROBE_INTERVAL=9, PROBES=5)
    - Two constructors: config Hash (initiator) and pre-connected TCPSocket (server-spawned)
    - connect(): TCP connection with configurable timeout, TCP_NODELAY, keepalive options
    - reconnect(): reconnection loop with RECONNECT_WAIT delay and max_reconnect_tries limit
    - process_outgoing(): HDLC or KISS framing with txb tracking and parent_interface propagation
    - process_incoming(): rxb tracking with parent_interface propagation, callback invocation
    - read_loop(): fiber-based socket reader with HDLC and KISS deframing
    - HDLC read: frame_buffer accumulation, FLAG-to-FLAG extraction, unescape, HEADER_MINSIZE filter
    - KISS read: FEND/FESC/TFEND/TFESC handling, CMD_DATA filtering, port nibble stripping
    - teardown(): close socket, mark offline, remove from parent spawned_interfaces
    - detach(): graceful shutdown with detached flag
    - to_s: "TCPInterface[name/ip:port]" with IPv6 bracket wrapping
    - Workaround for Crystal 1.19.1 compiler bug: used while-loop instead of each iterator
      for KISS byte processing (reassigning iteration variables causes compiler crash)
  - TCPServerInterface class inheriting from Interface:
    - Constants: BITRATE_GUESS (10_000_000), DEFAULT_IFAC_SIZE (16), AUTOCONFIGURE_MTU (true)
    - Constructor: config Hash with listen_ip, listen_port, port, i2p_tunneled, prefer_ipv6
    - accept_loop(): fiber-based connection acceptor using TCPServer.accept
    - incoming_connection(): spawns TCPClientInterface per client with:
      - Parent interface property propagation (IFAC, announce rate, mode, HW_MTU)
      - Remote IP/port tracking, spawned_interfaces management
    - clients: returns count of spawned interfaces
    - received_announce/sent_announce: only records from spawned interfaces (from_spawned flag)
    - process_outgoing: no-op (data sent through spawned client interfaces)
    - detach(): close server, mark offline
    - to_s: "TCPServerInterface[name/ip:port]" with IPv6 bracket wrapping
  - Updated src/rns.cr to require interfaces/tcp_interface
  - Created spec/rns/interfaces/tcp_interface_spec.cr (77 examples)
    - KISS module: constants (2), escape (5), unescape (3), roundtrip (1), frame (3) = 14 tests
    - TCPInterfaceConstants: HW_MTU (1 test)
    - TCPClientInterface constants: BITRATE_GUESS, IFAC_SIZE, AUTOCONFIGURE_MTU, reconnect,
      TCP keepalive, I2P, INITIAL_CONNECT_TIMEOUT (7 tests)
    - Constructor from config: connection, direction flags, KISS framing, I2P tunneled,
      connect_timeout, max_reconnect_tries (6 tests)
    - Constructor from socket: creation, callback (2 tests)
    - process_outgoing HDLC: framed data, txb tracking, parent txb (3 tests)
    - process_outgoing KISS: framed data (1 test)
    - HDLC framing roundtrip: send/receive, multiple frames, special bytes (3 tests)
    - KISS framing roundtrip: send/receive, special bytes (2 tests)
    - Bidirectional: both directions (1 test)
    - Reconnection: server restart, max_reconnect_tries (2 tests)
    - Teardown: close socket, remove from parent (2 tests)
    - Detach: marks detached and offline (1 test)
    - Remote close: non-initiator teardown (1 test)
    - Offline: no-op when offline (1 test)
    - to_s: formatted string, IPv6 brackets (2 tests)
    - Interface base class: inheritance, HW_MTU, hash, counters (4 tests)
    - Stress: 30 HDLC send/receive, 20 KISS send/receive (2 tests)
    - TCPServerInterface constants: BITRATE_GUESS, IFAC_SIZE, AUTOCONFIGURE_MTU (3 tests)
    - Server constructor: creation, port default, no port error, no IP error,
      direction flags, HW_MTU, spawned_interfaces (7 tests)
    - Incoming connections: accept client, multiple clients, receive data, parent rxb (4 tests)
    - Announce tracking: from_spawned flag (1 test)
    - Server process_outgoing: no-op (1 test)
    - Server detach: close server, idempotent (2 tests)
    - Server to_s: formatted string (1 test)
    - Server Interface base: inheritance (1 test)
    - Server stress: 10 rapid connections, 20 multi-client messages (2 tests)
  - Ran crystal spec — 1446 examples, 0 failures, 5 pending

2026-03-11 — Task 7.4: Local interface (client and server)
  - Created src/rns/interfaces/local_interface.cr (~270 LOC)
    porting RNS/Interfaces/LocalInterface.py (472 LOC)
  - LocalClientInterface class inheriting from Interface:
    - Constants: RECONNECT_WAIT (8), AUTOCONFIGURE_MTU (true), HW_MTU_DEFAULT (262144)
    - Two constructors: initiator (connects via target_port or socket_path) and
      server-spawned (wraps already-connected Socket)
    - Supports TCP localhost (127.0.0.1) and Unix domain socket connections
    - HDLC framing for all data (always HDLC, unlike TCP which supports KISS)
    - Reconnection logic for shared instance connections with Transport notification
    - should_ingress_limit? always returns false (local interfaces are trusted)
    - force_bitrate option with mutex-protected simulated latency
    - is_connected_to_shared_instance flag for reconnect/teardown behavior
    - Calls Transport.shared_connection_disappeared/reappeared
    - process_incoming with rxb tracking and parent_interface propagation
    - process_outgoing with HDLC framing, txb tracking, error-triggered teardown
    - read_loop fiber with HDLC deframing via process_hdlc_buffer
    - teardown with nowarning option, removes from parent spawned_interfaces
    - detach with socket cleanup
    - Bitrate: 1 Gbps (1_000_000_000)
    - to_s: "LocalInterface[port]" or "LocalInterface[path]"
  - LocalServerInterface class inheriting from Interface:
    - Two constructors: TCP (bindport) and Unix domain socket (socket_path)
    - TCPServer on 127.0.0.1 with reuse_port, accept_loop fiber
    - UNIXServer with stale socket file cleanup
    - incoming_connection_tcp/incoming_connection_unix: spawns LocalClientInterface
      per client with parent propagation (direction, bitrate, force_bitrate)
    - Tracks client count, parent/child interface relationships via spawned_interfaces
    - Announce tracking with from_spawned flag (only tracks spawned announces)
    - process_outgoing is no-op (server doesn't send directly)
    - detach cleans up socket file for Unix domain sockets
    - is_local_shared_instance flag (default true)
    - to_s: "Shared Instance[port]" or "Shared Instance[path]"
  - Added Transport.shared_connection_disappeared and shared_connection_reappeared
    stub methods to src/rns/transport.cr
  - Updated src/rns.cr to require interfaces/local_interface
  - Created spec/rns/interfaces/local_interface_spec.cr (49 examples)
    - LocalClientInterface constants (3 tests)
    - Connected socket constructor: TCP, Unix, TCP_NODELAY (3 tests)
    - TCP port constructor: connects to localhost (1 test)
    - Unix socket constructor: connects to Unix domain socket (1 test)
    - HDLC outgoing: receive framed data, frame outgoing data (2 tests)
    - HDLC roundtrip: bidirectional, special bytes, multiple messages (3 tests)
    - Byte counters: rxb/txb tracking (1 test)
    - should_ingress_limit?: always false (1 test)
    - Teardown: sets offline, removes from parent spawned_interfaces (2 tests)
    - Detach: marks detached, closes socket (1 test)
    - Remote close: detects closed socket (1 test)
    - to_s: TCP port, Unix path, no port (3 tests)
    - Base class: inheritance, direction flags (2 tests)
    - force_bitrate: defaults false (1 test)
    - process_outgoing when offline: drops silently (1 test)
    - Stress: 50 rapid sequential messages (1 test)
    - LocalServerInterface TCP constants (2 tests)
    - TCP server constructor: creation, spawned_interfaces, direction flags (3 tests)
    - TCP incoming connections: accept client, multiple clients, receive data,
      parent_interface set (4 tests)
    - Announce tracking: received from_spawned, not from_spawned, sent from_spawned (3 tests)
    - process_outgoing no-op (1 test)
    - Detach: stops accepting (1 test)
    - to_s: Shared Instance format (1 test)
    - Base class: inheritance (1 test)
    - Unix socket server: constructor, incoming connections, to_s, detach cleanup (4 tests)
    - Multi-client communication: independent routing (1 test)
    - Stress: 5 clients x 10 messages (1 test)
  - Ran crystal spec — 1495 examples, 0 new failures, 5 pending (pre-existing)

2026-03-11 — Task 7.5: Auto interface (peer discovery)
  - Created src/rns/interfaces/netinfo.cr — Network interface enumeration module
    - LibC bindings for getifaddrs, freeifaddrs, if_nametoindex, inet_ntop
    - AddressInfo record matching Python netinfo dict format
    - interfaces(): list all network interface names via POSIX getifaddrs
    - interface_name_to_index(): maps interface name to OS index
    - interface_name_to_nice_name(): returns interface name (POSIX: same as name)
    - interface_names_to_indexes(): hash mapping all names to indexes
    - ifaddresses(): returns addresses grouped by AF_INET/AF_INET6
    - format_ipv6(): formats 16 bytes as compressed IPv6 string with :: compression
    - descope_linklocal(): removes %ifname suffix and embedded scope specifiers
    - Raw byte extraction from sockaddr structures (offset 8 for IPv6, offset 4 for IPv4)
  - Created src/rns/interfaces/auto_interface.cr (~730 LOC)
    porting RNS/Interfaces/AutoInterface.py (663 LOC)
  - AutoInterface class inheriting from Interface:
    - Constants: HW_MTU=1196, FIXED_MTU=true, DEFAULT_DISCOVERY_PORT=29716,
      DEFAULT_DATA_PORT=42671, DEFAULT_GROUP_ID="reticulum", DEFAULT_IFAC_SIZE=16
    - Scope constants: SCOPE_LINK("2"), SCOPE_ADMIN("4"), SCOPE_SITE("5"),
      SCOPE_ORGANISATION("8"), SCOPE_GLOBAL("e")
    - Multicast address types: PERMANENT("0"), TEMPORARY("1")
    - Timing: PEERING_TIMEOUT=22.0, ANNOUNCE_INTERVAL=1.6, PEER_JOB_INTERVAL=4.0,
      MCAST_ECHO_TIMEOUT=6.5
    - Platform ignore lists: ALL_IGNORE_IFS, DARWIN_IGNORE_IFS, ANDROID_IGNORE_IFS
    - Multi-interface dedup: MULTI_IF_DEQUE_LEN=48, MULTI_IF_DEQUE_TTL=0.75
    - IPv6 multicast socket options: IPPROTO_IPV6, IPV6_JOIN_GROUP, IPV6_MULTICAST_IF
      with platform-specific values for macOS and Linux
    - Constructor: parses config Hash (name, group_id, discovery_scope, discovery_port,
      multicast_address_type, data_port, devices, ignored_devices, configured_bitrate),
      computes group hash and multicast address, enumerates interfaces, starts discovery
    - compute_mcast_address(): derives IPv6 multicast address from group hash matching
      Python format exactly (ff + type + scope + hash-derived groups)
    - start_discovery_listeners(): creates unicast and multicast UDP sockets per interface,
      joins multicast group via LibC.setsockopt, spawns discovery handler fibers
    - start_data_listener(): creates UDP server socket per interface for data transfer
    - discovery_handler(): receives peering packets, validates hash against group_id+addr,
      calls add_peer on valid packets
    - peer_jobs(): periodic fiber — timeout detection, reverse peering, multicast echo
      monitoring, carrier state tracking
    - announce_handler(): periodic multicast peering announcement per interface
    - peer_announce(): sends multicast discovery token (full_hash of group_id + link-local addr)
    - reverse_announce(): sends unicast discovery token to specific peer
    - add_peer(): creates AutoInterfacePeer with full IFAC setup (ifac_key via HKDF with
      Reticulum.IFAC_SALT, ifac_identity, ifac_signature), registers with Transport
    - refresh_peer(): updates last_heard timestamp
    - process_incoming(): routes data to spawned peer by address
    - mif_deque_add/mif_deque_hit?: multi-interface deduplication with TTL-bounded deque
    - ipv6_pton(): converts IPv6 string to 16-byte binary (Socket::IPAddress + manual fallback)
    - expand_ipv6(): expands compressed IPv6 to 8 groups
    - should_skip_interface?(): platform-specific interface filtering
  - AutoInterfacePeer class inheriting from Interface:
    - Constructor: owner, addr, ifname, inherits HW_MTU and parent_interface
    - process_incoming(): deduplication via owner mif_deque, rxb tracking on both
      peer and owner, refreshes peer timestamp, calls owner_inbound callback
    - process_outgoing(): mutex-protected UDP send with lazy socket/addr_info creation,
      txb tracking on both peer and owner
    - detach(): sets offline and detached flags
    - teardown(): removes from owner spawned interfaces, deregisters from Transport,
      sets direction flags to false, logs error or verbose based on detached state
    - to_s: "AutoInterfacePeer[ifname/addr]"
  - Added Reticulum.IFAC_SALT constant (32-byte hex) matching Python exactly
  - Added Reticulum.panic_on_interface_error class variable with getter/setter
  - Updated src/rns.cr to require netinfo and auto_interface modules
  - Created spec/rns/interfaces/auto_interface_spec.cr (105 examples)
    - NetInfo: format_ipv6 (7 tests: all-zeros, loopback, fe80, full address, longest
      zero run, leading zeros, single zero group, wrong size)
    - NetInfo: descope_linklocal (4 tests: %ifname, embedded scope, no scope, both)
    - NetInfo: interfaces (3 tests: non-empty, loopback present, unique names)
    - NetInfo: interface_name_to_index (2 tests: loopback positive, non-existent zero)
    - NetInfo: ifaddresses (4 tests: loopback has addresses, IPv4 127.0.0.1, IPv6 ::1,
      non-existent empty)
    - NetInfo: interface_name_to_nice_name (1 test), interface_names_to_indexes (1 test)
    - AutoInterface constants (11 tests: HW_MTU, FIXED_MTU, ports, IFAC size, scopes,
      multicast types, timing, platform ignore lists, bitrate, dedup constants)
    - AutoInterface constructor (16 tests: defaults, custom group_id/ports/scope/
      address_type, configured_bitrate, reverse_peering_interval, allowed/ignored
      interfaces parsing, empty peers)
    - Group hash and multicast address (5 tests: hash computation, format validation,
      scope changes, address type changes, group_id changes)
    - Peer management (7 tests: add_peer, own address multicast echo, existing peer
      refresh, refresh_peer timestamp, non-existent peer graceful, process_incoming
      routing, unknown peer ignore, offline ignore)
    - Multi-interface deduplication (4 tests: duplicate rejection, different data allowed,
      mif_deque_add TTL, unknown hash)
    - Detach (2 tests: online false, detached flag)
    - to_s (1 test), interface base class (4 tests), adopted/link-local tracking (2 tests)
    - Multicast echo tracking (3 tests), timed out interfaces (2 tests)
    - process_outgoing no-op (1 test), Reticulum constants (2 tests)
    - Stress (3 tests: 50 peers, 100 dedup entries, 20 instances)
    - AutoInterfacePeer constructor (4 tests: creation, HW_MTU, parent, inheritance)
    - AutoInterfacePeer process_incoming (5 tests: rxb tracking, dedup via owner,
      offline peer, offline owner, peer refresh on incoming)
    - AutoInterfacePeer detach/teardown (4 tests: flags, owner removal, Transport removal,
      direction flags)
    - AutoInterfacePeer to_s (1 test)
    - AutoInterfacePeer stress (2 tests: 30 messages, 20 create/teardown cycles)
  - Ran crystal spec — 1600 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 7.6: Serial interface
  - Ported RNS/Interfaces/SerialInterface.py (228 LOC) → src/rns/interfaces/serial_interface.cr
  - Implemented SerialInterface class with POSIX termios for serial port control:
    - Baud rate, databits, parity (none/even/odd), stopbits configuration
    - HDLC framing for data transmission and reception (uses existing HDLC module)
    - Byte-by-byte read loop matching Python's readLoop exactly
    - Timeout-based stale data clearing (100ms default)
    - Automatic reconnection on port errors (5-second retry interval)
    - HW_MTU = 564 matching Python
  - Used POSIX termios via Crystal's LibC bindings (tcgetattr, tcsetattr, cfmakeraw)
    plus LibSerial for cfsetispeed, cfsetospeed, tcflush
  - Added SerialConstants module with platform-specific termios constants
    (baud rates, control flags, character sizes, flow control)
  - Fixed pre-existing compilation issues in netinfo.cr:
    - LibC::Char → UInt8, LibC::UInt → UInt32, LibC::Int → Int32
    - LibC::Sockaddr → custom Sockaddr struct in LibNet
    - LibC::AF_INET/AF_INET6 → Socket::Family enum values
  - Updated src/rns.cr with require for serial_interface
  - Wrote 40 specs covering:
    - Constants (MAX_CHUNK, DEFAULT_IFAC_SIZE, parity symbols)
    - Constructor with explicit params (default/custom settings, parity parsing)
    - Configuration parsing (missing port error, default values)
    - should_ingress_limit? always returns false
    - to_s formatting, inheritance from Interface
    - teardown/detach behavior
    - process_outgoing (offline guard), process_incoming (rxb, callback)
    - HDLC framing roundtrip (frame/unescape, escaping FLAG/ESC bytes)
    - HDLC read loop simulation (single frame, multiple frames, special bytes,
      MTU truncation, empty frames, 100 random packets)
    - Pipe-based I/O simulation (multi-packet, byte-by-byte delivery)
    - SerialConstants verification
    - get_hash consistency
    - Stress test: 1000 random packets through HDLC state machine
  - Ran crystal spec — 1640 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 7.7: KISS and AX.25 KISS interfaces
  - Added extended KISS command constants to existing KISS module in interface.cr:
    - CMD_TXDELAY (0x01), CMD_P (0x02), CMD_SLOTTIME (0x03), CMD_TXTAIL (0x04),
      CMD_FULLDUPLEX (0x05), CMD_SETHARDWARE (0x06), CMD_READY (0x0F), CMD_RETURN (0xFF)
  - Created src/rns/interfaces/kiss_interface.cr porting RNS/Interfaces/KISSInterface.py (387 LOC)
    - KISSInterface class inheriting from Interface
    - Constants: MAX_CHUNK=32768, BITRATE_GUESS=1200, DEFAULT_IFAC_SIZE=8, HW_MTU=564
    - Serial port management via POSIX termios (reuses LibSerial/SerialConstants from serial_interface)
    - KISS TNC configuration: set_preamble, set_tx_tail, set_persistence, set_slot_time, set_flow_control
    - KISS-framed read loop with byte-by-byte parsing, FEND/FESC/TFEND/TFESC escape handling
    - Port nibble stripping (supports multi-port TNCs)
    - CMD_READY flow control: interface_ready flag, packet queue, flow_control_timeout
    - Beacon/identification support: beacon_interval, beacon_data, first_tx tracking,
      minimum 15-byte padding for beacon frames
    - Reconnection on serial port errors (5-second retry interval)
    - process_outgoing with KISS framing via KISS.frame()
    - process_incoming with rxb tracking and callback invocation
    - should_ingress_limit? always returns false
    - Pipe-based testing support via serial_io accessor
  - Created src/rns/interfaces/ax25_kiss_interface.cr porting RNS/Interfaces/AX25KISSInterface.py (400 LOC)
    - AX25 module with protocol constants:
      - PID_NOLAYER3 (0xF0), CTRL_UI (0x03), CRC_CORRECT, HEADER_SIZE (16)
      - encode_call(): AX.25 address encoding (6-char shifted callsign + SSID byte)
      - build_header(): constructs 16-byte AX.25 header (dst + src + CTRL + PID)
    - AX25KISSInterface class inheriting from Interface
    - Same serial port/KISS TNC management as KISSInterface
    - AX.25 addressing: src_call, src_ssid, dst_call ("APZRNS"), dst_ssid
    - Callsign validation: 3-6 chars, uppercase, SSID 0-15
    - process_outgoing: builds AX.25 header + data, then KISS frames the combined payload
    - process_incoming: strips 16-byte AX.25 header, rejects frames <= HEADER_SIZE
    - KISS read loop with MTU limit adjusted for AX.25 header (HW_MTU + HEADER_SIZE)
    - Flow control, reconnection, same patterns as KISSInterface
  - Updated src/rns.cr to require kiss_interface and ax25_kiss_interface
  - Created spec/rns/interfaces/kiss_interface_spec.cr (99 examples)
    - KISS extended constants: all 14 command constants (1 test)
    - KISS escape: FEND, FESC, both, empty, no special bytes (5 tests)
    - KISS unescape: TFEND, TFESC, both (3 tests)
    - KISS frame: wrapping, escaping, 100 random roundtrips (3 tests)
    - KISSInterface constants: MAX_CHUNK, BITRATE_GUESS, IFAC_SIZE, parity symbols (2 tests)
    - Constructor: defaults, custom KISS params, flow control, beacon config, parity parsing (5 tests)
    - Config hash: missing port error, config key parsing (2 tests)
    - should_ingress_limit? always false (1 test)
    - to_s formatting (1 test)
    - Interface base class: inheritance, counters, HW_MTU, hash (4 tests)
    - Teardown/detach (2 tests)
    - process_outgoing: offline guard, queue when not ready (2 tests)
    - process_incoming: rxb tracking, callback (1 test)
    - Flow control: queue, process_queue pop, process_queue sets ready (3 tests)
    - KISS frame encoding: data, FEND escape, FESC escape (3 tests)
    - KISS read loop: single frame, multiple frames, special bytes, CMD_READY,
      port nibble stripping, MTU limit, 100 random roundtrips (7 tests)
    - KISS command encoding: preamble, txtail, persistence, slottime, clamping (5 tests)
    - Beacon: config storage, first_tx nil, tracking (3 tests)
    - Pipe I/O: KISS framed send, escaped send (2 tests)
    - Stress: 1000 random KISS state machine roundtrips (1 test)
    - AX25 constants (1 test)
    - AX25 encode_call: 6-char, short padding, last bit, no last bit (4 tests)
    - AX25 build_header: size, dst/src order, CTRL/PID, last bit, Python encoding match (5 tests)
    - AX25KISSInterface constants (1 test)
    - Constructor: defaults, uppercase, callsign min/max, SSID range, all SSIDs,
      custom KISS params, parity parsing (8 tests)
    - Config hash: missing port error (1 test)
    - should_ingress_limit?, to_s, base class, counters, hash (5 tests)
    - Teardown/detach (2 tests)
    - process_incoming: header stripping, short frame rejection, exact header rejection (3 tests)
    - process_outgoing: offline guard, queue, AX.25 header + KISS framing via pipe (3 tests)
    - Flow control: queue, process_queue (2 tests)
    - AX.25 KISS read loop: header stripping, header-only rejection (2 tests)
    - AX.25 address formatting: dst callsign, default destination (2 tests)
    - Stress: 100 AX.25 KISS pipeline roundtrips, 20 interfaces with valid callsigns (2 tests)
  - Ran crystal spec — 1739 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 7.8: Backbone interface
  - Created src/rns/interfaces/backbone_interface.cr
    - BackboneInterfaceConstants module with HW_MTU = 1048576
    - BackboneInterface (server): TCP listener accepting backbone connections
      - Constants: BITRATE_GUESS = 1_000_000_000, DEFAULT_IFAC_SIZE = 16, AUTOCONFIGURE_MTU = true
      - Configuration from Hash: name, listen_ip, listen_port, port (fallback), device, prefer_ipv6
      - get_address_for_if: resolves bind address from network interface name via NetInfo
      - accept_loop: fiber-based connection acceptance
      - incoming_connection: spawns BackboneClientInterface per connection, inherits parent properties
        (ifac, announce rates, mode, MTU, bitrate, direction flags)
      - clients: returns count of spawned interfaces
      - received_announce/sent_announce: tracks frequency only from spawned interfaces
      - process_outgoing: no-op (server does not transmit directly)
      - detach: closes server socket, marks offline
      - to_s: BackboneInterface[name/ip:port] with IPv6 bracket formatting
    - BackboneClientInterface: HDLC-framed TCP client for backbone links
      - Constants: BITRATE_GUESS = 100_000_000, RECONNECT_WAIT = 5, TCP socket options
        (TCP_USER_TIMEOUT = 24, TCP_PROBE_AFTER = 5, TCP_PROBE_INTERVAL = 2, TCP_PROBES = 12),
        INITIAL_CONNECT_TIMEOUT = 5, SYNCHRONOUS_START = true
      - Two constructors: from config Hash (initiator) or from pre-connected TCPSocket (spawned)
      - connect: establishes TCP connection with timeout, TCP_NODELAY, keepalive
      - reconnect: automatic reconnection loop for initiator clients with max retry support
      - HDLC framing: process_outgoing wraps data in FLAG+escape+FLAG, write_mutex for fiber safety
      - read_loop: fiber-based async reading with HDLC deframing via process_hdlc_buffer
      - process_incoming: tracks rxb on both client and parent interface
      - teardown: marks offline, removes self from parent spawned_interfaces, closes socket
      - detach: marks detached, closes socket
      - to_s: BackboneInterface[name/ip:port] with IPv6 bracket formatting
  - Updated src/rns.cr to require interfaces/backbone_interface
  - Created spec/rns/interfaces/backbone_interface_spec.cr (35 examples)
    - BackboneInterface constants (4 tests), initialization (4 tests), server behavior (2 tests),
      to_s formatting (2 tests), announce tracking (2 tests), detach (1 test)
    - BackboneClientInterface constants (6 tests), spawned client (1 test),
      HDLC framing (2 tests), HDLC deframing (3 tests), server+client integration (2 tests),
      initiator client (1 test), process_incoming (1 test), teardown (2 tests), to_s (2 tests)
  - Ran crystal spec — 1774 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 7.9: PipeInterface with subprocess pipe communication
  - Created src/rns/interfaces/pipe_interface.cr — PipeInterface class for subprocess I/O
    - Spawns external processes via Process.new with shell: true, piped stdin/stdout
    - HDLC framing for packetization (reuses existing RNS::HDLC module)
    - Constants: MAX_CHUNK (32768), BITRATE_GUESS (1_000_000), DEFAULT_IFAC_SIZE (8), HW_MTU (1064)
    - open_pipe: spawns subprocess with Process.new, captures stdin/stdout pipes
    - configure_pipe: starts read_loop fiber, sets online
    - process_outgoing: HDLC-frames data and writes to subprocess stdin
    - read_loop: byte-by-byte HDLC state machine reading from subprocess stdout
    - reconnect_pipe: periodic respawn attempts with configurable respawn_delay
    - kill_process: graceful process termination
    - teardown/detach: clean shutdown
    - Configuration from Hash: name, command, respawn_delay (default 5.0)
    - Test constructor with spawn_process: false for unit testing without subprocess
  - Updated src/rns.cr to require interfaces/pipe_interface
  - Created spec/rns/interfaces/pipe_interface_spec.cr (33 examples)
    - Constants tests (3): MAX_CHUNK, BITRATE_GUESS, DEFAULT_IFAC_SIZE
    - Constructor tests (2): default settings, custom respawn_delay
    - Configuration parsing (2): missing command raises, default respawn_delay
    - Inheritance tests (2): is_a Interface, has Interface properties
    - process_incoming tests (3): rxb counter, inbound callback, accumulation
    - process_outgoing (1): does nothing when offline
    - teardown/detach (2): sets online false, marks detached
    - to_s (1): PipeInterface[name] format
    - get_hash (2): consistent for same name, different for different names
    - HW_MTU (1): defaults to 1064
    - HDLC framing (4): FLAG bytes, FLAG escaping, ESC escaping, roundtrip
    - Subprocess communication (5): single packet via cat, multiple packets,
      HDLC special bytes, process termination, 10 random packets roundtrip
    - HDLC state machine (5): single frame, multiple frames, HW_MTU truncation,
      empty frames, 500 random packet roundtrip
  - Ran crystal spec — 1807 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 7.10: I2P interface
  - Created src/rns/interfaces/i2p_interface.cr (~600 LOC)
    porting RNS/Interfaces/I2PInterface.py (1009 LOC)
  - I2PExceptions module with all SAM protocol error types:
    - I2PError (base), CantReachPeer, DuplicatedDest, DuplicatedId, InvalidId,
      InvalidKey, KeyNotFound, PeerNotFound, Timeout
    - from_sam_result: parses SAM reply strings to exception types
  - Base32 module: RFC 4648 encoder for I2P address computation
  - SAMClient class for SAM v3 protocol communication:
    - connect: TCP connection + HELLO handshake (MIN=3.1 MAX=3.1)
    - generate_destination: DEST GENERATE for keypair creation
    - create_session: SESSION CREATE STYLE=STREAM for tunnel sessions
    - stream_connect: STREAM CONNECT for outbound I2P connections
    - stream_accept: STREAM ACCEPT for inbound I2P connections
    - naming_lookup: NAMING LOOKUP for destination resolution
    - Constants: DEFAULT_SAM_HOST (127.0.0.1), DEFAULT_SAM_PORT (7656)
  - I2PController class managing I2P tunnel lifecycle:
    - Storage directory management (rns_storagepath/i2p)
    - get_free_port: ephemeral port allocation via TCPServer bind
    - client_tunnel: SAM session + stream connect for outbound tunnels
    - server_tunnel: key file management (old/new format paths), SAM session
      creation, accept loop fiber for incoming connections, b32 address
    - Unique session IDs via atomic counter + timestamp
    - Exception logging with SAM error type classification
    - start/stop lifecycle management
  - I2PInterfacePeer class inheriting from Interface:
    - Constants: RECONNECT_WAIT=15, I2P_USER_TIMEOUT=45, I2P_PROBE_AFTER=10,
      I2P_PROBE_INTERVAL=9, I2P_PROBES=5, I2P_READ_TIMEOUT=110
    - Tunnel states: TUNNEL_STATE_INIT (0x00), ACTIVE (0x01), STALE (0x02)
    - Three constructors: connected socket (server-spawned), target I2P dest
      (initiator with tunnel_job + wait_job fibers), test (no I2P needed)
    - IFAC inheritance from parent interface (ifac_netname/netkey → HKDF key)
    - process_outgoing: mutex-protected HDLC or KISS framing, parent txb tracking
    - process_incoming: rxb tracking, parent rxb when parent_count=true
    - read_loop: byte-by-byte HDLC/KISS deframing matching Python exactly
    - read_watchdog: fiber-based tunnel health monitoring, keepalive sending,
      stale detection (PROBE_AFTER*2), timeout (I2P_READ_TIMEOUT)
    - connect: socket creation with timeout, socket option configuration
    - reconnect: loop with RECONNECT_WAIT delay, max_reconnect_tries support
    - teardown: offline, direction flags cleared, parent cleanup, panic support
    - detach: sets detached flag, closes socket
    - HW_MTU = 1064 matching Python
  - I2PInterface class (server) inheriting from Interface:
    - Constants: BITRATE_GUESS=256000, DEFAULT_IFAC_SIZE=16
    - Two constructors: config Hash (full with SAM+peers) and test (name only)
    - TCP server with accept_loop fiber for incoming connections
    - incoming_connection: spawns I2PInterfacePeer with full property inheritance
      (IFAC, announce rates, mode, HW_MTU, bitrate, direction flags)
    - server_tunnel_job: retry loop for connectable I2P endpoint setup
    - Peer configuration: comma-separated peers string, each gets I2PInterfacePeer
    - clients: count of spawned interfaces
    - received_announce/sent_announce: only tracks from_spawned (matching Python)
    - process_outgoing: no-op (data through spawned peers)
    - detach: stops I2P controller, closes server
    - HW_MTU = 1064, supports_discovery = true
  - Updated src/rns.cr to require interfaces/i2p_interface
  - Created spec/rns/interfaces/i2p_interface_spec.cr (102 examples)
    - I2PExceptions: from_sam_result for all 9 error types + OK + inheritance (12 tests)
    - Base32: empty, single byte, RFC test vectors f/fo/foo/foob/fooba/foobar (7 tests)
    - SAMClient: constants (1), default settings (1), custom settings (1)
    - I2PController: storage dir creation (1), start/stop (1), free port (1),
      unique ports (1), empty tunnel tables (1), custom SAM (1), to_s (1)
    - I2PInterfacePeer constants: reconnect (2), I2P timeouts (4), READ_TIMEOUT
      computation (1), tunnel states (1)
    - Test constructor: name, non-initiator, initiator, HW_MTU, HDLC/KISS framing,
      i2p_tunneled, MODE_FULL, bitrate (9 tests)
    - Connected socket constructor: creation (1), IFAC inheritance (1)
    - process_incoming: rxb tracking (1), callback (1), parent rxb with
      parent_count true/false (2 tests)
    - process_outgoing: offline guard (1), HDLC framing (1), KISS framing (1),
      parent txb tracking (1)
    - HDLC framing roundtrip: single frame (1), multiple frames (1),
      HDLC special bytes (1)
    - KISS framing roundtrip: single frame (1), KISS special bytes (1)
    - Teardown: sets offline (1), clears direction flags (1), removes from parent (1)
    - Detach: sets flag (1), closes socket (1)
    - to_s: I2PInterfacePeer[name] (1)
    - Tunnel state: init (1), active transition (1), stale transition (1)
    - Interface base class: inheritance (1), byte counters (1), hash consistency (1)
    - Reconnection properties: defaults (3 tests)
    - Stress: 50 HDLC send/receive cycles (1), 20 create/teardown cycles (1)
    - I2PInterface constants: BITRATE_GUESS (1), DEFAULT_IFAC_SIZE (1)
    - Test constructor: name (1), inheritance (1), HW_MTU (1), MODE_FULL (1),
      starts offline (1), supports discovery (1), empty spawned (1),
      direction flags (1), bitrate (1), nil b32 (1), has I2PController (1)
    - Clients: count (1)
    - incoming_connection: spawns peer (1), correct properties (1)
    - process_outgoing: no-op (1)
    - Announce tracking: received from_spawned (1), sent from_spawned (1)
    - Detach: stops controller (1)
    - to_s: I2PInterface[name] (1)
    - Connectable property (2 tests), b32 property (1 test)
    - Stress: 20 create/teardown (1), 10 incoming connections (1)
  - Ran crystal spec — 1909 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 7.11: RNode interface (LoRa)
  - Created src/rns/interfaces/rnode_interface.cr (~850 LOC)
    porting RNS/Interfaces/RNodeInterface.py (1558 LOC)
  - RNodeKISS module with all RNode-specific KISS command constants:
    - Frame delimiters: FEND (0xC0), FESC (0xDB), TFEND (0xDC), TFESC (0xDD)
    - Radio config commands: CMD_FREQUENCY (0x01), CMD_BANDWIDTH (0x02),
      CMD_TXPOWER (0x03), CMD_SF (0x04), CMD_CR (0x05)
    - Device commands: CMD_RADIO_STATE (0x06), CMD_RADIO_LOCK (0x07),
      CMD_DETECT (0x08), CMD_LEAVE (0x0A), CMD_READY (0x0F),
      CMD_ST_ALOCK (0x0B), CMD_LT_ALOCK (0x0C)
    - Statistics commands: CMD_STAT_RX (0x21), CMD_STAT_TX (0x22),
      CMD_STAT_RSSI (0x23), CMD_STAT_SNR (0x24), CMD_STAT_CHTM (0x25),
      CMD_STAT_PHYPRM (0x26), CMD_STAT_BAT (0x27), CMD_STAT_CSMA (0x28),
      CMD_STAT_TEMP (0x29)
    - Display/framebuffer: CMD_BLINK (0x30), CMD_RANDOM (0x40),
      CMD_FB_EXT (0x41), CMD_FB_READ (0x42), CMD_FB_WRITE (0x43),
      CMD_DISP_READ (0x66)
    - Device management: CMD_BT_CTRL (0x46), CMD_PLATFORM (0x48),
      CMD_MCU (0x49), CMD_FW_VERSION (0x50), CMD_ROM_READ (0x51),
      CMD_RESET (0x55)
    - Detection: DETECT_REQ (0x73), DETECT_RESP (0x46)
    - Radio states: RADIO_STATE_OFF (0x00), RADIO_STATE_ON (0x01),
      RADIO_STATE_ASK (0xFF)
    - Error codes: CMD_ERROR (0x90), ERROR_INITRADIO through ERROR_MODEM_TIMEOUT
    - Platform types: PLATFORM_AVR (0x90), PLATFORM_ESP32 (0x80),
      PLATFORM_NRF52 (0x70)
  - RNodeTCPConnection class for TCP network connections to RNode:
    - Constants: TARGET_PORT=7633, CONNECT_TIMEOUT=5.0, RECONNECT_WAIT=4.0,
      ACTIVITY_TIMEOUT=6.0, ACTIVITY_KEEPALIVE=3.5
    - TCP keepalive: TCP_USER_TIMEOUT=24, TCP_PROBE_AFTER=5,
      TCP_PROBE_INTERVAL=2, TCP_PROBES=12
    - connect: TCP connection with timeout, TCP_NODELAY, platform-specific
      keepalive (Linux: TCP_USER_TIMEOUT/KEEPIDLE/KEEPINTVL/KEEPCNT,
      macOS: TCP_KEEPALIVE)
    - initial_connect: spawns read_loop fiber on success
    - read_loop: fiber-based async reading, feeds data to owner
    - write: sends directly when connected, queues when disconnected
    - read: mutex-protected byte queue consumption
    - close/cleanup: graceful shutdown
  - RNodeInterface class inheriting from Interface:
    - Constants: MAX_CHUNK=32768, DEFAULT_IFAC_SIZE=8, HW_MTU=508,
      FREQ_MIN=137MHz, FREQ_MAX=3GHz, RSSI_OFFSET=157,
      CALLSIGN_MAX_LEN=32, REQUIRED_FW_VER_MAJ=1, REQUIRED_FW_VER_MIN=52,
      RECONNECT_WAIT=5
    - Signal quality: Q_SNR_MIN_BASE=-9, Q_SNR_MAX=6, Q_SNR_STEP=2
    - Battery states: BATTERY_STATE_UNKNOWN/DISCHARGING/CHARGING/CHARGED
    - Display: FB_PIXEL_WIDTH=64, FB_BYTES_PER_LINE=8, DISPLAY_READ_INTERVAL=1.0
    - Two constructors: config Hash (full with port parsing) and explicit params
      (for testing without hardware)
    - Port URI parsing: serial (/dev/...), tcp:// (RNodeTCPConnection),
      ble:// (stub, logged as unsupported)
    - Configuration validation: frequency (137MHz-3GHz), txpower (0-37),
      bandwidth (7.8kHz-1.625MHz), spreading factor (5-12), coding rate (5-8),
      airtime limits (0-100%), callsign length (<=32 bytes)
    - Radio parameter commands: set_frequency (4-byte BE), set_bandwidth (4-byte BE),
      set_tx_power (1 byte), set_spreading_factor (1 byte), set_coding_rate (1 byte),
      set_st_alock/set_lt_alock (2-byte xx.xx format), set_radio_state
    - init_radio: sends all configuration in Python-matching order
    - detect: sends DETECT_REQ + FW_VERSION + PLATFORM + MCU requests
    - configure_device: reset_radio_state, start read_loop, detect, init_radio,
      validate_radio_state, set online — with TCP/BLE/serial detect timeouts
    - validate_firmware: checks maj_version/min_version >= 1.52, panics if not
    - validate_radio_state: compares configured vs reported parameters with tolerances
    - update_bitrate: SF * ((4.0/CR) / (2^SF / (BW/1000))) * 1000
    - read_loop: complete KISS state machine with escape handling for all command types:
      - CMD_DATA: data frame → process_incoming
      - CMD_FREQUENCY/CMD_BANDWIDTH: 4-byte BE → update r_frequency/r_bandwidth + bitrate
      - CMD_TXPOWER/CMD_SF/CMD_CR: 1 byte → update reported values + bitrate
      - CMD_RADIO_STATE/CMD_RADIO_LOCK: 1 byte → update state
      - CMD_FW_VERSION: 2 bytes → validate_firmware
      - CMD_STAT_RX/CMD_STAT_TX: 4-byte BE → packet counts
      - CMD_STAT_RSSI: 1 byte → byte - 157 = dBm
      - CMD_STAT_SNR: signed byte * 0.25 → quality % computation
      - CMD_STAT_CHTM: 11 bytes → airtime/channel load/RSSI/noise/interference
      - CMD_STAT_PHYPRM: 12 bytes → symbol time/rate/preamble/CSMA params
      - CMD_STAT_CSMA: 3 bytes → contention window band/min/max
      - CMD_STAT_BAT: 2 bytes → battery state/percent (clamped 0-100)
      - CMD_STAT_TEMP: 1 byte → byte-120 celsius (-30 to 90 range)
      - CMD_RANDOM/CMD_PLATFORM/CMD_MCU: 1 byte → device info
      - CMD_ERROR: error code → raise/log per type
      - CMD_RESET: ESP32 online reset detection → reinit
      - CMD_READY: process_queue for flow control
      - CMD_FB_READ: 512 bytes → framebuffer
      - CMD_DISP_READ: 1024 bytes → display data
      - CMD_DETECT: DETECT_RESP → detected=true
      - Timeout: resets state after 100ms/1250ms/1500ms (serial/BLE/TCP)
      - ID beacon: transmits callsign after id_interval from first_tx
      - TCP keepalive: sends detect every ACTIVITY_KEEPALIVE seconds
    - process_incoming: rxb tracking, callback, clears RSSI/SNR
    - process_outgoing: KISS frame encoding, flow control, ID beacon tracking
    - queue/process_queue: packet queuing with flow control
    - Display operations: enable/disable_external_framebuffer, display_image,
      write_framebuffer, read_framebuffer, read_display, start/stop_display_updates
    - Device management: hard_reset, leave, detect
    - Battery: get_battery_state, get_battery_state_string, get_battery_percent
    - Reconnection: reconnect_port with RECONNECT_WAIT delay
    - teardown/detach: graceful shutdown with radio off + leave
    - Serial port: POSIX termios at 115200 baud, 8N1
  - Updated src/rns.cr to require interfaces/rnode_interface
  - Created spec/rns/interfaces/rnode_interface_spec.cr (95 examples)
    - RNodeKISS constants: frame delimiters (1), RNode commands (1),
      statistics commands (1), device management (1), detection (1),
      radio states (1), error codes (1), platform types (1), escape (1) = 9 tests
    - RNodeInterface constants: hardware/frequency (1), signal quality (1),
      firmware version (1), battery states (1), display (1), callsign/reconnect (1) = 6 tests
    - Test constructor: defaults (1), HW_MTU (1), supports_discovery (1),
      radio state off (1), flow control (1), ID beacon (1), airtime limits (1),
      empty queue (1), empty hw_errors (1) = 9 tests
    - Config parsing: missing port raises (1), TCP URI (1), BLE URI (1) = 3 tests
    - Validation: frequency min/max (2), txpower min/max (2), bandwidth min/max (2),
      SF min/max (2), CR min/max (2), airtime limits (2), valid config (1),
      all valid SF (1), all valid CR (1) = 17 tests
    - should_ingress_limit? always false (1)
    - to_s formatting (1)
    - Interface base class: inheritance (1), byte counters (1), hash (1) = 3 tests
    - reset_radio_state (1)
    - Battery: state (1), unknown string (1), charged (1), charging (1),
      discharging (1), percent (1) = 6 tests
    - update_bitrate: calculation (1), nil params (1), SF7/BW125k/CR5 (1),
      lower for higher SF (1) = 4 tests
    - process_incoming: rxb (1), callback (1), clears RSSI/SNR (1) = 3 tests
    - process_outgoing: offline guard (1), queues when not ready (1) = 2 tests
    - queue/process_queue: queuing (1), sets ready (1) = 2 tests
    - validate_firmware: major > required (1), exact (1), higher minor (1) = 3 tests
    - teardown/detach (1)
    - KISS command encoding: frequency roundtrip (1), bandwidth encoding (1),
      airtime limit encoding (1), detect command (1) = 4 tests
    - KISS data frame: encoding (1), escape FEND (1), escape FESC (1),
      100 random roundtrips (1) = 4 tests
    - KISS read loop simulation: data frame (1), frequency response (1),
      detect response (1), firmware version (1), RSSI (1), SNR + quality (1),
      platform (1), battery (1), temperature (1), multiple frames (1),
      escaped bytes (1) = 11 tests
    - validate_radio_state: match (1), frequency mismatch (1) = 2 tests
    - RNodeTCPConnection constants (2 tests)
    - Stress: 20 configurations (1), 100 random KISS roundtrips (1),
      50 frequency roundtrips (1) = 3 tests
  - Ran crystal spec — 2004 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 7.12: RNode Multi and Weave interfaces
  - Ported RNodeMultiInterface.py (1148 LOC) → src/rns/interfaces/rnode_multi_interface.cr (~1190 lines)
    - RNodeMultiKISS module: all KISS constants, CMD_SEL_INT, CMD_INTERFACES, CMD_INT0_DATA-CMD_INT11_DATA, chip type constants, interface_type_to_str, is_data_cmd?, escape
    - SubIntConfig record: name, vport, frequency, bandwidth, txpower, sf, cr, flow_control, st_alock, lt_alock, outgoing
    - RNodeMultiInterface class: serial port management, detect/configure, read_loop with full KISS command parsing, subinterface creation, framebuffer support, firmware validation
    - RNodeSubInterface class: rnode_parent typed reference, radio parameter validation (frequency range per chip type, bandwidth, txpower, sf, cr, airtime limits), bitrate calculation, queue management, process_incoming/process_outgoing
  - Ported WeaveInterface.py (1091 LOC) → src/rns/interfaces/weave_interface.cr (~1090 lines)
    - WeaveWDCL module: HDLC frame type constants (FLAG=0x7E, ESC=0x7D)
    - WeaveCmd/WeaveEvt modules: command and event constants with description lookups
    - WeaveLogFrame class, WeaveEndpoint class with bounded receive queue
    - WeaveDevice class: identity management, discovery, handshake, stats tracking, endpoint management, incoming_frame parsing, log_handle
    - WeaveWDCLConnection class: serial connection, HDLC framing, read_loop, reconnection
    - WeaveInterface class: peer management via peer_spawned_interfaces, spawned interfaces, peering timeout jobs
    - WeaveInterfacePeer class: per-peer packet processing with deduplication via mif_deque
  - Updated src/rns.cr to require both new interfaces
  - Wrote 126 specs across both files (55 + 71 examples), all passing
  - Key fixes: rnode_parent typed property, peer_spawned_interfaces rename, firmware validation logic, Int64 bitrate alignment
  - Ran crystal spec — 2130 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 8.1: Configuration parser
  - Created src/rns/vendor/config_obj.cr — ConfigObj parser compatible with Python's configobj
    - ConfigObj class with Section inner class for nested config structure
    - Value union type: String | Array(String) | Section
    - Parsing: INI-like format with bracket-depth sections ([section], [[subsection]], etc.)
    - Section marker regex and key=value regex matching Python configobj patterns
    - Nested section support: child, sibling, and depth walk-back for returning to parent levels
    - Comment handling: skip comment-only lines, strip inline comments (quote-aware)
    - List value parsing: comma-separated values with quote-aware splitting
    - Empty list support (single comma), trailing comma single-element lists
    - Quoted value/section name support (single and double quotes)
    - Type conversion: as_bool (yes/no/on/off/true/false/1/0), as_int, as_float, as_list
    - Boolean conversion is case-insensitive, matching Python configobj._bools exactly
    - Dictionary-like access: [], []=, has_key?, get with default, size
    - Section tracking: separate scalars and sections lists for ordered iteration
    - File I/O: from_file(path) reads config, write(path) writes config
    - Write-back: generates INI format with proper bracket depth and indentation
    - add_section for programmatic section creation
  - Created spec/rns/vendor/config_obj_spec.cr with 51 test examples covering:
    - Empty config, key=value parsing, whitespace handling, quoted values
    - Comment lines, blank lines, inline comments, empty string values
    - Top-level sections, nested sections (double brackets), deeply nested sections
    - Sibling sections, section depth tracking, section iteration
    - Boolean conversion (all variants), integer/float conversion, list conversion
    - Dictionary access (has_key?, get with default, []=, scalars/sections tracking)
    - File I/O (read and write roundtrip)
    - RNS test config parsing (reticulum/logging/interfaces sections with type coercion)
    - RNS default config parsing (with interface subsections)
    - Depth walk-back (going from nested back to root), quoted section names
    - List values (comma-separated, quoted, empty list, trailing comma)
    - Programmatic config setting and section creation
  - Updated src/rns.cr to require vendor/config_obj
  - Ran crystal spec — 2181 examples (51 new), 0 failures in config_obj specs

2026-03-11 — Task 8.2: Reticulum main class — initialization and configuration
  - Ported RNS/Reticulum.py (1716 LOC, part 1) → src/rns/reticulum.cr (~870 LOC)
  - Reticulum module with all protocol constants matching Python exactly:
    - MTU=500, LINK_MTU_DISCOVERY=true, MAX_QUEUED_ANNOUNCES=16384,
      QUEUED_ANNOUNCE_LIFE=86400, ANNOUNCE_CAP=2, MINIMUM_BITRATE=5,
      DEFAULT_PER_HOP_TIMEOUT=6, TRUNCATED_HASHLENGTH=128
    - HEADER_MINSIZE=19, HEADER_MAXSIZE=35, IFAC_MIN_SIZE=1, MDU=464
    - IFAC_SALT (32 bytes matching Python hex exactly)
    - Time intervals: RESOURCE_CACHE=86400, JOB_INTERVAL=300, CLEAN_INTERVAL=900,
      PERSIST_INTERVAL=43200, GRACIOUS_PERSIST_INTERVAL=300
    - Default ports: DEFAULT_LOCAL_INTERFACE_PORT=37428, DEFAULT_LOCAL_CONTROL_PORT=37429
  - Singleton management: @@instance, get_instance, reset_instance!, class_set_instance
  - Configuration directory detection (4-level priority matching Python):
    1. Explicit configdir parameter
    2. /etc/reticulum (if dir + config file exist)
    3. ~/.config/reticulum (if dir + config file exist)
    4. ~/.reticulum (fallback)
  - Path management: configdir, configpath, storagepath, cachepath, resourcepath,
    identitypath, blackholepath, interfacepath — all with class-level getters/setters
  - Directory creation: storage, cache, resources, identities, blackhole, interfaces,
    cache/announces subdirectories created at initialization
  - ReticulumInstance class with full initialization sequence:
    - Singleton guard (raises on double-init)
    - Log destination configuration (LOG_FILE, LOG_CALLBACK)
    - Loglevel clamping (0-7), verbosity additive to config loglevel
    - Config loading from file via ConfigObj, or default config creation
    - apply_config: parses [logging] and [reticulum] sections
  - Config parsing for all reticulum options:
    - share_instance, instance_name, shared_instance_type, shared_instance_port,
      instance_control_port, rpc_key (hex), enable_transport, network_identity,
      link_mtu_discovery, enable_remote_management, remote_management_allowed (ACL),
      respond_to_probes, force_shared_instance_bitrate, panic_on_interface_error,
      use_implicit_proof, discover_interfaces, required_discovery_value,
      publish_blackhole, blackhole_sources, interface_discovery_sources,
      autoconnect_discovered_interfaces
  - AF_UNIX vs TCP determination matching Python platform logic
  - Default config template (DEFAULT_RNS_CONFIG) matching Python's __default_rns_config__
    with [reticulum], [logging], and [interfaces] sections including Default Interface
  - start_local_interface: shared instance (LocalServerInterface) / client
    (LocalClientInterface) / standalone mode with proper fallback chain
  - Background jobs fiber: periodic cache cleaning and data persistence
  - Cache cleaning: resource cache (RESOURCE_CACHE expiry) and packet cache
    (DESTINATION_TIMEOUT expiry) with file modification time checks
  - Exit handler: detach_interfaces, Transport.exit_handler, Identity.exit_handler
  - Signal handlers: SIGINT, SIGTERM → detach interfaces
  - Static accessors for all class-level flags: transport_enabled?, link_mtu_discovery?,
    remote_management_enabled?, should_use_implicit_proof?, probe_destination_enabled?,
    discovery_enabled?, discover_interfaces?, required_discovery_value,
    publish_blackhole_enabled?, blackhole_sources, interface_discovery_sources,
    should_autoconnect_discovered_interfaces?, max_autoconnected_interfaces
  - Added Transport stubs: detach_interfaces, remote_management_allowed (with dedup),
    add_remote_management_allowed, cleared in reset
  - Added Identity.try_clean_ratchets stub
  - Created spec/rns/reticulum_spec.cr (106 examples)
    - Protocol constants (13 tests): MTU, LINK_MTU_DISCOVERY, MAX_QUEUED_ANNOUNCES,
      QUEUED_ANNOUNCE_LIFE, ANNOUNCE_CAP, MINIMUM_BITRATE, DEFAULT_PER_HOP_TIMEOUT,
      TRUNCATED_HASHLENGTH, HEADER_MINSIZE, HEADER_MAXSIZE, IFAC_MIN_SIZE, MDU, IFAC_SALT
    - Time intervals (5 tests): RESOURCE_CACHE, JOB_INTERVAL, CLEAN_INTERVAL,
      PERSIST_INTERVAL, GRACIOUS_PERSIST_INTERVAL
    - Default ports (2 tests)
    - Path management (4 tests): default, set/read, all accessors, mutation
    - Singleton management (2 tests): nil after reset, reset clears state
    - Static accessors (18 tests): all flags with defaults and set/get
    - Default config template (14 tests): lines, sections, values, ConfigObj parsing,
      reticulum/logging/interfaces content, Default Interface subsection
    - Default config file I/O (2 tests): write to disk, read back
    - Config parsing (25 tests): loglevel, verbosity, clamping, share_instance,
      shared_instance_port, instance_control_port, enable_transport,
      panic_on_interface_error, use_implicit_proof, discover_interfaces,
      required_discovery_value, publish_blackhole, force_shared_instance_bitrate,
      shared_instance_type, respond_to_probes, enable_remote_management,
      autoconnect_discovered_interfaces, AF_UNIX/TCP, local_socket_path,
      instance_name, rpc_key valid/invalid, empty config
    - Full initialization (7 tests): directory creation, singleton rejection,
      default config creation, standalone mode, loglevel from config/override/clamp
    - Exit handler (3 tests): runs, idempotent, signal handlers
    - Transport stubs (3 tests): add/dedup remote management, detach_interfaces
    - Stress (3 tests): 20 configs, 50 resets, 30 default config parses
  - Ran crystal spec — 2287 examples, 0 failures, 5 pending (pre-existing)

2026-03-11 — Task 8.3: Reticulum main class — interface instantiation and lifecycle
  - Implemented interface instantiation from config in src/rns/reticulum.cr:
    - start_system_interfaces: iterates [[interface_name]] sections, detects duplicates
    - synthesize_interface: parses all config parameters (mode, IFAC, ingress control,
      bitrate, announce rate, discovery settings), instantiates correct Interface subclass
    - interface_post_init: applies post-creation config (mode, bitrate, IFAC key derivation,
      announce rate, ingress control, discovery properties), registers with Transport
    - Supports all 14 interface types: AutoInterface, BackboneInterface,
      BackboneClientInterface, UDPInterface, TCPServerInterface, TCPClientInterface,
      I2PInterface, SerialInterface, PipeInterface, KISSInterface, AX25KISSInterface,
      RNodeInterface, RNodeMultiInterface, WeaveInterface
    - BackboneInterface config alias normalization (port→listen_port/target_port, remote→target_host)
    - I2PInterface gets storagepath/ifac_netname/ifac_netkey/ifac_size injected
    - RNodeMultiInterface gets start() called after post_init
  - Implemented add_interface public API method for programmatic interface addition
    (matching Python's _add_interface), including IFAC key derivation
  - Implemented should_persist_data? helper using GRACIOUS_PERSIST_INTERVAL
  - Added 12 discovery properties to Interface base class: discovery_announce_interval,
    discovery_publish_ifac, reachable_on, discovery_name, discovery_encrypt,
    discovery_stamp_value, discovery_latitude/longitude/height,
    discovery_frequency/bandwidth/modulation
  - Added ConfigObj::Section#to_string_hash for converting config sections to Hash(String, String)
  - Fixed pre-existing compilation issues:
    - I2PInterface: nilable ifac_size assignment, nilable transport_identity.hash
    - RNodeMultiInterface: Crystal 1.19.1 compiler bug with .try blocks inside
      while+rescue (refactored to if-checks), missing dir_out/dir_in on RNodeSubInterface,
      nilable hw_mtu comparison, RNS.hexrep(byte) type mismatch
    - WeaveInterface/RNodeMultiInterface: LibC::O_NOCTTY, cfsetispeed/cfsetospeed/tcflush
      and VMIN/VTIME/CS8/CREAD/CLOCAL not in Crystal LibC (use LibSerial/SerialConstants)
    - WeaveInterface: Slice copy_to argument mismatch
  - Added 25 new specs to spec/rns/reticulum_spec.cr covering:
    - Interface instantiation from config: mode constants, disabled interfaces, duplicate
      names, unknown types, IFAC settings, announce rate settings, interface mode parsing,
      bitrate config, discovery settings, ingress control, bootstrap_configs tracking
    - interface_post_init: DEFAULT_IFAC_SIZE verification across all interface types
    - add_interface API: shared instance guard, should_persist_data? timing
    - Exit handler: safe calls, idempotent, signal handlers
    - ConfigObj::Section#to_string_hash: conversion and non-string filtering
    - Interface discovery properties: defaults and setters on base class
    - start_system_interfaces: no interfaces section, empty section, discovery flag
  - Ran crystal spec — 2312 examples, 0 failures, 1 error (pre-existing UDP port bind),
    5 pending

2026-03-11 — Task 8.4: Discovery module
  - Created src/rns/discovery.cr — port of RNS/Discovery.py (734 LOC)
    - InterfaceAnnouncer: periodic discovery announce generation for local interfaces
    - InterfaceAnnounceHandler: receives and validates discovery announces
    - InterfaceDiscovery: coordinates discovery, persistence, autoconnect
    - BlackholeUpdater: fetches and merges blackhole identity lists
    - Helper functions: is_ip_address?(), is_hostname?()
  - Added interface properties: autoconnect_hash, autoconnect_source, autoconnect_down, discovery_channel
  - Added Transport.interface_objects registry for Discovery iteration
  - Updated src/rns.cr to require discovery module
  - Created spec/rns/discovery_spec.cr (49 examples)
  - Ran crystal spec — 2361 examples, 49/49 discovery specs pass,
    3 pre-existing flaky failures in stress tests, 5 pending
